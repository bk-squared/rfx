__pycache__/
*.pyc
*.egg-info/
.DS_Store
.pytest_cache/
.omc/
.omx/
.rfx-experiments/
.playwright-cli/
build/
output/
studio/node_modules/
studio/*.tsbuildinfo
docs-site/
CLAUDE.md
AGENTS.md
.claude/

# docs/ publication boundaries live in docs/.gitignore

# Internal scripts (debugging, infra sync, GPU validation)
scripts/validate_codex_fixes.py
scripts/gpu_verify_issues.py
scripts/check_public_docs_*.py
scripts/export_public_docs_to_gitops.py
scripts/test_patch_nonuniform.py
scripts/validate_new_features.py
scripts/generate_field_snapshots.py

# VESSL cluster configs (internal infra)
**/vessl*.yaml
**/vessl*.yml

# Scratch / reference examples (not part of rfx)
forge_example/

# Dev trial/slop examples
examples/benchmark_jax_vs_numpy.py
examples/gpu_benchmark.py
examples/gpu_benchmark_results.json

# Generated artifacts (runtime output — never commit)
dist/
logs/
# Exception (PR #548 review): committed external-referee run logs are
# EVIDENCE, not runtime output -- REPRODUCE_GATE_RECORD's log_path must
# be git-tracked so a reviewer outside this machine can open it (the
# whole point of the fill-contract test in
# tests/test_coax_two_port_referee_header.py). The broad "logs/" rule
# above would otherwise silently swallow this directory too.
!validation/research/coax_two_port/logs/
!validation/research/coax_two_port/logs/*
**/*.png
!docs/guide/images/*.png
!docs/public/examples/assets/*.png
**/*.gif
**/benchmark_results.json
examples/*.json

# VESSL cluster log mirrors (operational; not physics evidence)
docs/vessl-logs/
scripts/diagnostics/_artifacts/*.npz

# PyPI upload credentials — never commit
.pypi
.pypirc

# Gallery v3 figure-builder intermediate field caches (regenerated on demand)
scripts/_patch_v3_cache.npz
scripts/_patch_field_cache.npz
