# rfx Validation Scripts

These scripts compare rfx results with analytic calculations, other solvers,
or a second rfx configuration. They are test cases, not tutorials. Start with
`examples/README.md` if you are learning the software.

Each comparison covers the geometry, mesh, frequency range, source, and output
implemented by that script. A passing result does not establish accuracy for a
different setup.

## Run a validation case

Run scripts from the repository root. The straight-waveguide case is the
shortest place to start after completing the examples:

```bash
python validation/crossval/03_straight_waveguide_flux.py
```

The process exit code has a specific meaning:

- `0`: every configured numerical check passed, including a required external
  comparison when the case has one.
- `1`: a simulation, numerical check, or required execution step failed.
- `2`: the rfx checks completed, but a required reference file or external
  solver was unavailable. This result is inconclusive, not a pass.

See `crossval/manifest.json` for each script's dependencies, expected exit
codes, numerical-check files, and generated artifacts.

## Available cases

| Script | What it checks | Reference and interpretation limit |
| --- | --- | --- |
| `crossval/01_waveguide_bend.py` | 2-D dielectric-bend transmission and straight-guide normalization | Meep and unit transmission; applies to the scripted geometry and band |
| `crossval/02_ring_resonator.py` | 2-D ring-resonator mode frequencies and mode `Q` | Meep. The mode correspondence is a tolerance-free one-to-one assignment (`comparators/ring_mode_judge.py`), decoupled from the verdict: through 2026-08 the matcher admitted a pair only within 5% and the headline gate then averaged that same quantity at 5%, so the verdict was entailed by the matcher and 200,000 random trials through it never produced a failure (#812). Gated now: no unmatched Meep mode, at least two modes, mean and per-mode frequency error under the same published 5%, and `Q` within `tau_ref/T` — the record-length resolution limit — for each mode whose decay the record observed (`T/tau >= 1/4`; on the shipped run that gates the 0.118 and 0.147 modes and excludes the 0.175 mode, whose `T/tau = 0.096` would make a Q gate a measurement of run length). Says nothing about mode field patterns, which the script renders but does not gate. |
| `crossval/03_straight_waveguide_flux.py` | Straight-guide guided-mode dispersion (E2) plus flux and band-mean transmission (E1) | Closed-form slab TE0 `n_eff` for the declared recipe, Meep, and the unit-transmission identity within the measured window |
| `crossval/04_multilayer_fresnel.py` | Normal-incidence multilayer reflection, transmission, and energy balance | Transfer-matrix calculation and Meep over the scripted band. Both references decide the verdict as of #812 — the analytic slab through a fringe-resolved gate on each Fabry–Perot extremum's position and value (the band-mean gates it sits beside passed while the true `eps` was 12.33% away), and Meep through pointwise `max\|Meep − analytic\| ≤ 0.08` / `max\|rfx − Meep\| ≤ 0.16` plus its own fringe positions. Before #812 the E4 leg was an `import` check: every Meep number was printed and none was gated. Meep is absent from the PR/CPU runners, so the Meep leg is exercised only by the scheduled `crossval-external` workflow job. Continuum gate + exact-lattice witness at every rung (docs/design_notes/20260903_lattice_witness_standard.md): the witness is REPORTED, not gated, at this case's committed 719-step record — it does not settle to −40 dB, so the derived `W_witness` is `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_W_witness_R_gated = 0.0535` in R and `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_W_witness_T_gated = 0.178` in T, looser than the case's own windows, and neither the wrong-model nor the 1 % ε′ falsifier fires there. The settled rung for the same slab is cv23's `sigma_zero` arm, where |rfx − lattice| = 1.7e-4 against `W_witness` 5.5e-3 — **and there this case's committed band-mean `|ΔR|` of 0.0066 IS the slab's Yee-lattice second-order term. That identification is scoped to `|ΔR|`.** The lattice term is the same number in R and T (0.00727 over this case's 170-bin mask, because the slab is lossless), but the committed envelopes are not: 0.0066 in R (lattice/envelope 1.10) against 0.011 in T (0.66), and at cv04's own rung |rfx − lattice| is `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_dR_lattice_gated = 0.00168` in R but `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_dT_lattice_gated = 0.00625` in T — so more than half of the T envelope is this record's truncation, and `W_mean,T = 0.017`, derived from it, is NOT a discretisation number. Gate test `tests/crossval/test_lattice_witness_gates.py`. |
| `crossval/05_patch_antenna.py` | 2.4 GHz finite-ground-plane patch resonance (coarse-mesh integration check) | Diagnostic-reporter only: reports a coarse-mesh resonance vs a Balanis estimate and OpenEMS, both reported not gated. Its 20% openEMS check is a smoke bound that passes over two different substrate geometries (#325), so it does not establish accuracy. Since issue #812 (2026-09-01) the reported resonance is the mode identified as TM100 of the declared spectrum rather than the argmin nearest the analytic anchor, so a mis-realized length fails by name (a 22.0 mm patch reads `tests/fixtures/patch_mode_identification/cv05_ringdown_spectra.json::runs.patch_len_22p0mm.modes[0].freq = 2.993459 GHz`, assigned TM110, FAIL) instead of being re-anchored; the identification window [-11.11 %, +12.50 %] is not an accuracy claim. One falsifier fired against the instrument: at 38.0 mm a weak extra pole (`tests/fixtures/patch_mode_identification/cv05_ringdown_spectra.json::runs.patch_len_38p0mm.modes[1].freq = 2.609612 GHz`) inside the window is named TM100 on the committed cluster-built fixture, so a spurious low-amplitude pole in the design window is accepted — recorded, not tuned away. The committed patch evidence — regression locks and signed-envelope characterizations, not accuracy claims (issue #782; the harminv gate's own docstring says so) — lives in `tests/locks/test_patch_edgefed_resonance_harminv.py`, `tests/locks/test_patch_edgefed_s11_passivity.py`, `tests/oracle/test_patch_cavity_eps_oracle.py`, and `tests/crossval/test_patch_canonical_farfield_e4.py` (committed thirds-rule OpenEMS far-field reference: directivity within 1.0 dB, resonance inside the documented +6% to +16% coarse-mesh band with the sign locked HIGH — a regression lock, not an accuracy statement. **Correction 2026-08-31** (#812 Phase 0, documentation only; no gate value was changed): this cell previously read "directivity within 0.5 dB, resonance inside the documented −12% to −5%". Those were the pre-#693 envelopes, pinned on the wrong member of the patch's near-degenerate TM pair — the 40 mm cross mode (2.2147 GHz) rather than the 32 mm feed-axis design mode that the committed openEMS 2.4221 GHz reference is. The test was repinned on 2026-08-27 (PR #716, commit 12d2f00) to `F_RES_REL_LO = +0.06`, `F_RES_REL_HI = +0.16`, `D_ABS_TOL_DB = 1.0`; this row was not updated with it, so it kept quoting the retired band with the sign flipped. Authoritative source: `tests/crossval/test_patch_canonical_farfield_e4.py:117,121,122` (`D_ABS_TOL_DB`, `F_RES_REL_LO`, `F_RES_REL_HI`) — cite the names, not only the lines. **Correction 2026-08-31** (same lane): this pointer read `:111-116`, which was stale in the very commit that wrote it (f88f992 added six docstring lines to the same file), so `:111` landed on an unrelated comment. `tests/contracts/test_evidence_citation_pointers.py` now gates it). |
| `crossval/06b_msl_notch_filter_uniform.py` | Uniform-mesh microstrip-notch response | The script checks a quarter-wave estimate at dx=63.5 µm = h_sub/4 (issue #723; it shipped at dx=80 µm through 2026-08, where the declared 254 µm substrate rasterized to 320 µm — the references described a board that mesh did not solve). The analytic reference is evaluated on the realized 635.0 µm trace width, read live from `sim.fidelity_report()`. A separate dx=50 µm OpenEMS result is checked by `tests/crossval/test_msl_notch_e4_comparison_gates.py`, and after #723 that comparison is on a DIFFERENT board — its rfx leg still realizes h_sub=300 µm. The shipped run is not an external-solver comparison. Neither establishes general MSL-port accuracy. Re-gated 2026-09-01 (issue #812) and judged on its own board 2026-09-02: all gates pass (`validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::criterion_A_baseline.err_pct = 1.453` %), but a one-cell stub-length falsifier FIRED (refined shift `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::stub_1cell.refined_delta_pct = 0.145` % of a predicted 0.532 %), so sub-bin notch resolution on this board is not demonstrated. |
| `crossval/07_sheen_lpf.py` | Sheen 1990 stepped-impedance microstrip low-pass filter stopband | Characterization study, not an accuracy comparison. A committed Palace FEM referee shows the stopband is a double transmission zero (~7.0 and ~8.0 GHz) that OpenEMS reproduces to about 0.7%. The original rfx leg (num_periods=20, default probe offset) was non-passive (58/120 bins, worst column power 1794.5) and its argmin sat 9.6% from OpenEMS; the root cause — record truncation (settling witness −24.7 dB) plus probe near-field contamination — was measured and the leg regenerated 2026-07-26 (num_periods=60, witness −72 dB; n_probe_offset=30, which satisfies both the 5·h_sub near-field and the λ_g/4 reflector clearances; passivity-enforced `\|S\|` strictly ≤ 1 with the raw excess recorded per bin). The rfx argmin now lands at 7.874 GHz, the ~8 GHz doublet member, 1.4% from OpenEMS, and the re-derived referee shows rfx resolving both doublet members (structure distance 1.52% vs OpenEMS 0.66%; sides_with = openems stands). Read narrowly still: no bin carries a `passivity_correction` > 0.05 on the current leg (0/120, worst 0.0145 at 17.38 GHz) — the pre-#516 leg's 84/120 bins, worst 0.365, was substantially extractor defects, not a coarse-mesh envelope — and the fitted passband `Re(Z₀)` reads 50.3 Ω vs OpenEMS's flat ~51 Ω (printed, not gated; the old ~67 Ω fit-conditioning residual is gone; the in-band 5–15 GHz median is 52.4 Ω); the null position is insensitive to this. The checks lock the OpenEMS reference's soundness, the comparison shape, the committed null frequencies, the rfx passband mean, the referee verdict, and the evidence chain (strict bound, settling witness, correction footprint; fail-closed if a leg lacks the fields). Re-gated 2026-09-01 (issue #812 mechanism P3, `docs/design_notes/estimator_resolution_regate.md`): the committed null was a bin argmin on a 163.866 MHz grid (2.081%/bin) judged against a declared 1% window, so erasing the lower doublet member left every gated number bit-identical and passed 17/17. The argmin lock is unchanged; added are sub-bin log-parabolic locks on both doublet members for both solvers (`±0.50%`, against the committed referee fixture's own `fdtd_doublet_ghz`), a transmission-zero COUNT gate (`== 2` at `≤ −20 dB`), a `−3 dB` corner-frequency lock (`±0.25%`) on a quantity the case never gated, and an in-run half-grid resolution witness. 28/28 pass on the committed legs; the erased-zero and corner-error falsifiers each fail exactly the gates they were built to fail (`scripts/diagnostics/cv07_estimator_falsifiers.py`). |
| `crossval/09_half_symmetric_waveguide.py` | PMC half-symmetry against a full cavity, plus the REALIZED mirror plane the PMC face lands on (issue #812) | Pozar TE101 frequency, a full-domain rfx calculation for the scripted mesh, and the realize-declared geometry budget `dx/4` |
| `crossval/10_pmc_cpml_half_symmetric.py` | Stability of combined PMC and CPML boundaries | rfx self-consistency check only; it is not an external-solver comparison. The peak-stability gate is a RELATIVE spread and therefore cannot see a constant amplitude factor: issue #812 measured that deleting `apply_pmc_faces` makes that score better (0.0667% vs 0.2487% against a 2% gate) while scaling the peaks by 0.5077. Two gates added 2026-08-31 close it — tangential H exactly `0.0` on the declared `y_lo` face at every swept `cpml_layers` on both paths, and the half-domain peak within 2% of the equivalent mirrored full-domain in-phase image-pair peak, which is the absolute amplitude the relative spread discards. See `docs/design_notes/cv10_pmc_realization_regate.md`. |
| `crossval/11_waveguide_port_wr90.py` | WR-90 empty guide, PEC short, and dielectric slab | Analytic checks, including the Airy slab result, determine the exit status. Optional Meep, OpenEMS, and Palace files add diagnostic table columns but are not required for exit code `0`. Use `docs/guides/sparameter_support_matrix.md` for the supported range and authoritative checks. **STALE (2026-08-31, #812 Phase 0) — quote its numbers with their date.** This case's only `artifact_paths` entry, `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json` — the E4-external leg of the `rectangular_waveguide_port` broad-E5 chain — is the 2026-06-16 number (`b0322c1`, PR #181) and has never been regenerated. Its **provenance is settled**: feeding `git show b0322c1:tests/fixtures/waveguide_broad_e5/cv11_wr90_fresh_stdout.txt` (that stdout as it stood at the artifact's own commit — the working-tree copy was overwritten later, at `20e5533`, 2026-08-28, #724/#730) to the artifact's own builder (`scripts/diagnostics/build_waveguide_wr90_rectangular_broad_e4_comparison.py --reference-column Palace_r_h2`; pure text post-processing, no FDTD) rebuilds it field for field — 54 numeric fields, 52 bit-identical, 2 differing at ~1 ulp. What is wrong with it is its **age**: the same builder on the three current committed stdouts rebuilds slab `S11` `max_mag_abs_diff` to `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::provenance.staleness_2026_08_31.summary_max_mag_abs_diff.rebuilt_from_main_baseline_stdout = 0.0186` / `0.0194` / `0.0193` against the artifact's `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::pairs[3].max_mag_abs_diff = 0.0707` (~3.7x better), and slab `S11` `mean_mag_abs_diff` to `0.007705`--`0.007771` against `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::pairs[3].mean_mag_abs_diff = 0.043976`; the Palace reference column is identical throughout, so the delta is entirely on the rfx leg. Direction matters — the artifact **understates** the family, `0.0707` sits inside its own `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::max_mag_abs_tol = 0.1`, no gate is at risk, and cv11's analytic (E2) checks, which determine its exit status, are untouched and unaffected. **Correction:** this row said PROVENANCE-DISPUTED earlier on 2026-08-31 (commit `2d05212`), on the claim that the rfx leg reproduces from no committed run of its own script; that is withdrawn — the check behind it rebuilt only from the working-tree revision of the sibling stdouts. What remains open is only whether to **refresh** the artifact, and any refresh must *explain* the ~3.7x delta rather than silently re-pin it. See `docs/design_notes/20260831_cv11_broad_e4_artifact_provenance.md`. |
| `crossval/14_rect_cavity_pozar.py` | Rectangular air-filled PEC cavity eigenfrequencies | Exact closed-form Pozar cavity resonance, re-derived in the script; no external solver and no meshing tolerance enter the oracle. TE₁₀₁ is checked to 1% and **every** one of the seven declared modes to 2% (max, not min; a mode that cannot be extracted is a hard failure), for one 50×30×40 mm cavity on a dx = 1 mm mesh that divides all three extents exactly. Two further gates were added in the #812 re-gate: wall registration `|(n−1)·dx − (a,b,d)| ≤ 1e-9 m`, and a residual against a second in-file exact oracle — the closed-form discrete-Yee eigenvalue of the same box — to one tenth of the record's Fourier bin. The cavity is closed and lossless, so no settling witness applies and the harminv `Q` is a window-length artifact that is printed but not checked. Says nothing about open boundaries, lossy or dispersive fills, non-uniform meshes, or other geometries. |
| `crossval/24_nu_rect_cavity_pozar.py` | Rectangular air-filled PEC cavity eigenfrequencies on GRADED-z meshes (cv14's cavity: uniform 1 mm control, one graded band, small–large–small–large, uniform 0.5 mm cost control) | Scope: one closed lossless PEC cavity (50 × 30 × 40 mm), z grading only, 0.5 / 1.0 mm cells with the chain 0.5–0.7–0.8–1.0 mm (ratio ≤ 1.4, the #785 envelope), the seven modes below 8.5 GHz; says nothing about S-parameters, ports, absorbers, materials or in-plane grading — the multi-band mesh's support-matrix row stays mesh-only for S-parameters. Exact closed-form Pozar spectrum re-derived in the comparator (E2) with the exact discrete Yee-lattice eigenfrequency of each arm's own grid as the a-priori witness, gated to 4 ppm (`validation/crossval/_24_nu_cavity_results/rfx.json::declared.w_est = 4e-06`; 1.5 × the model's worst residual against two committed TM111 anchors). Gated per arm: cv14's committed tolerance; the #810 allowance on the equal-dt spatial eigenvalue (allowance from #785's F-S2 reflection table at the fixture's own cells per axial wavelength); the lattice gate; mode count 7 by (m, n, l); #785's F-S1 energy envelope on the arm's own grid; two-window stationarity; envelope; realized extent. Pre-declared before the run in `docs/design_notes/20260902_cv24_nu_cavity_predeclaration.md`. Measured (VESSL 369367257837, committed under `_24_nu_cavity_results/`, every gate true on every arm, exit `validation/crossval/_24_nu_cavity_results/rfx.json::verdict.exit_code = 0`): lattice residual ≤ 0.48 ppm on every arm and mode (worst `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE102.resid_lattice = -0.00005 %` on the control's TE102) and ≤ 0.08 ppm on the graded arms (`validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TM210.resid_lattice = +0.00001 %`, `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.resid_lattice = -0.00001 %`); TE101 raw deviation `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE101.dev_raw = -0.00833 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE101.dev_raw = -0.01497 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE101.dev_raw = -0.01411 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform_fine.rows.TE101.dev_raw = -0.00205 %` on the four arms, TE102 `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE102.dev_raw = -0.05202 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE102.dev_raw = -0.08214 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.dev_raw = -0.01821 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform_fine.rows.TE102.dev_raw = -0.01294 %` — every one within 0.5 ppm of its pre-declared prediction (the risky one, arm c's TE102 predicted `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.pred_dev_lattice = -0.01820 %`, landed); allowance room used ≤ 17 %: at equal dt no l ≥ 1 mode on either graded arm has a positive excess over the control except the single band's TE102 (`validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE102.dev_spatial = -0.10157 %` vs `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE102.dev_spatial = -0.09091 %`, +107 ppm, 12 % worse), the two-band profile's TE102 is 532 ppm better (`validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.dev_spatial = -0.03768 %`), and its l = 1 modes are 5–11 ppm better than the control (TE101 `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE101.dev_spatial = -0.02099 %` vs `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE101.dev_spatial = -0.02210 %`), as the lattice predicted; stationarity ≤ `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform_fine.rows.TE102.stationarity = 0.00010 %`; energy drift ≤ `validation/crossval/_24_nu_cavity_results/rfx.json::arms.single_band.measured.energy.max_drift = 4.31e-08` against envelopes ≥ `validation/crossval/_24_nu_cavity_results/rfx.json::arms.uniform.measured.energy.envelope_at_end = 1.53e-04`. Cost (16-CPU pod): `validation/crossval/_24_nu_cavity_results/rfx.json::arms.multi_band.cost.n_cells = 73500` cells / `validation/crossval/_24_nu_cavity_results/rfx.json::arms.multi_band.cost.wall_run_s = 6.0` s FDTD for the small–large–small–large arm against `validation/crossval/_24_nu_cavity_results/rfx.json::arms.uniform_fine.cost.n_cells = 480000` / `validation/crossval/_24_nu_cavity_results/rfx.json::arms.uniform_fine.cost.wall_run_s = 34.9` s for uniform-fine — but uniform-fine is more accurate on every mode. Read plainly: the graded-z mesh is within the derived allowance of the uniform mesh it refines (equal dt: within 11 ppm on the l = 1 modes, +107 ppm worse on TE102 for the single band, 532 ppm better for the two-band profile; RAW, i.e. at each arm's own CFL dt: 60–300 ppm worse on every l ≤ 1 mode because the smaller dt shrinks the time-dispersion term that partly cancels the control's spatial one, e.g. TE101 `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE101.dev_raw = -0.01497 %` vs `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE101.dev_raw = -0.00833 %`), and the grading adds no error beyond the operator's own metric (lattice residual ≤ 0.08 ppm); it buys only the z-share of each mode's error (78 % of uniform-fine's TE102 improvement at 15 % of its cells, nothing on the l = 0 modes), so the 6–9× cost advantage holds only for z-dominated error. The allowance gate's bound contains the control's own dispersion, so it cannot fail for an in-envelope profile (it fires at ≳ 6–42 % of the CORE-C2 metric swap); the lattice gate (4 ppm, fires at ≳ 0.1 % of that swap) carries the whole mesh claim. Five falsifiers each exit 1 for the declared reason (ratio 2.0 → envelope only, PASSING the allowance gate as predicted a priori — the #810 allowance is a loose bound for eigenfrequencies; the pre-CORE-C2 metric swap → allowance + lattice, TE101 residual `validation/crossval/_24_nu_cavity_results/rfx__falsifier_metric_defect.json::evaluations.metric_defect.rows.TE101.resid_lattice = -0.35188 %`; grading at the wall → envelope; one extra fine cell → by name on the five l ≥ 1 modes; a dropped search band → count `validation/crossval/_24_nu_cavity_results/rfx__falsifier_mode_count_drop_te102.json::evaluations.mode_count_drop_te102.n_clusters_in_band = 6` ≠ 7). Rig check before the run: harminv's auto-decimation was a 10–20 ppm estimator floor and was switched off before any arm ran (note section 12). The Palace graded-dy WR-90 fixture is the E4 leg of a follow-up case, not this one. |
| `crossval/15_patch_antenna_rt5880.py` | Probe-fed patch on a 3.175 mm RT/Duroid 5880 laminate, uniform mesh | A same-geometry rfx-versus-OpenEMS integration study on a different laminate from `crossval/05_patch_antenna.py`, which it does not replace. It checks a coarse-mesh envelope (resonance within 8% of the OpenEMS dip, a −40 dB settling witness, passivity on both sides, broadside directivity within 3 dB) and asserts the substrate rasterizes to exactly four cells before quoting a resonance. Issue #812 (round 2): its 8 % f0 gate is blind to the #740 one-plane-ground realization (`validation/crossval/_15_patch_results/rfx_one_plane_ground_b29f9de7.json::f_harminv_hz = 2471858108` Hz vs `validation/crossval/_15_patch_results/openems.json::f_dip_hz = 2330000000` Hz, +6.09 %); only the build assertion catches it, and no ratio-band window with provenance could be derived — recorded as an honest STOP, gates unchanged. Patch accuracy is not established here; patch evidence remains with `crossval/05_patch_antenna.py` and the committed tests that case names (regression locks, not accuracy claims). |
| `crossval/16_pec_sphere_mie_ka_sweep.py` | PEC-sphere monostatic RCS vs exact Mie, ka 0.5–4.0 | Two-tier: coarse ka ≤ 1.25 and the fine rung at ka = 2.0 only are gated against a twice-re-implemented Mie series, at gate = round-up(measured clearance-scan envelope × 1.5) with the envelope population committed and recomputed by the gate test; coarse ka ≥ 1.5 and fine ka = 3.0/4.0 are reported but never gated — near the deep Mie nulls the monostatic value is domain-size unconverged (domain-to-domain spread 8.0 dB at coarse ka = 1.75 and 14.5 dB at fine ka = 3.0, worst single-point deviation 11.1 dB coarse / 9.3 dB fine; fine ka = 4.0 fails a 3.5 dB gate at 9 of 13 clearances in the committed anti-aliasing scan) and the minima positions move with domain size. Truncation (gated bins), domain and clearance witnesses are fixture data; the effective-radius and resolution-ladder probes are recorded as offline provenance, and incident leakage at the monostatic bin is excluded by the documented backscatter leakage null (the formerly-quoted sub-vs-unsub probe was retracted as a same-array tautology — `monostatic_rcs` is unsubtracted by construction). Says nothing about bistatic bins, dielectric spheres, or oblique incidence. |
| `crossval/17_dielectric_sphere_mie.py` | Lossless dielectric sphere (eps_r 2.56) monostatic RCS vs exact Mie, ka 0.5–2.5 | Material-path twin of the PEC ka-sweep; first cross-method record of the binary dielectric rasterize path (no sub-cell averaging exists — a staircase envelope, not a smooth-interface claim). Single gated tier: coarse ka ≤ 1.25 within 6.3 dB (committed 7-point clearance-scan envelope 4.18 dB × 1.5, round-up). No fine rung is gated — the committed cpr-12.8 witness scan shows doubled resolution does not buy a domain-robust tighter envelope (3.75/3.04 dB vs coarse 4.18). ka ≥ 1.5 is reported but never gated (domain-size-only spreads 11.7–29.0 dB). Says nothing about bistatic bins, lossy dielectrics, or smooth-interface accuracy. Re-gated 2026-09-01 (issue #812): the dB gate cannot see the permittivity — its model blind window is `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[0][0] = 2.0`–`validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[0][1] = 4.5` and `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[1][0] = 5.0`–`validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[1][1] = 5.6` (fail island 4.6–4.9 between them) — so the rasterized permittivity is gated on its own channel (G17-A within 0.5 % of 2.56, G17-B exactly two distinct values). |
| `crossval/18_wr90_iris_modematch.py` | Single symmetric inductive iris in WR-90 vs a TEn0 mode-matching oracle, 8.2–12.4 GHz | First calibrated PEC-obstacle evidence in the rectangular-waveguide lane (multi-iris filters stay experimental — this is the prerequisite stage). Gated (re-derived 2026-09-01, issue #812): fine rung dx = a/60 within a per-configuration window (round-up of each row's own envelope × 1.5, from `validation/crossval/_18_wr90_iris_results/aperture_resolution.json::pairs[2].fine_gate_abs = 0.015` to `validation/crossval/_18_wr90_iris_results/aperture_resolution.json::pairs[4].fine_gate_abs = 0.035` abs; the pooled 0.04 abs is kept as an outer bound) across eight configurations, so a one-cell aperture error is detected at every configuration (committed detection table in `validation/crossval/_18_wr90_iris_results/aperture_resolution.json`), plus a Richardson-extrapolation cross-check on the oracle within 0.01 abs at every fine/coarse pair; the first-order gap ratios (0.527–0.604) are the attribution witness. Coarse rung, raw extraction and residual ripple (measured against the oracle: fine ≤ 0.0077, coarse ≤ 0.0158) are reported but never gated. Three setup defects (parasitic wall-slot, half-ulp node-plane corners, an electrical aperture of d + 2·dx together with a too-thin absorber) were found and are each fenced; the earlier modal-extraction fence is retracted because its non-passivity evidence does not survive the corrected setup, with modal accuracy committed alongside (a little worse than flux, which keeps the gate). Says nothing about phase, multi-iris filters, posts, septa, or off-centre apertures. |
| `crossval/19_wr90_iris_filter_aghanim.py` | A published 4th-order WR-90 inductive-iris bandpass filter (five irises, four coupled cavities) vs a TEn0 mode-matching cascade oracle, 10.40–11.70 GHz | Diagnostic only — it reports a comparison and is not evidence for broader support. The reference HFSS/CST numbers are **digitized from the paper's figure**; no external solver is run here, and rfx is not compared against either commercial code. The first resonant multi-obstacle case in the rectangular-waveguide lane, where a per-face geometry error is a passband shift rather than a magnitude tolerance. Gated: centre frequency within `validation/crossval/_19_iris_filter_results/rfx.json::gates.f0_gate_mhz = 19.0` MHz (envelope `validation/crossval/_19_iris_filter_results/rfx.json::gates.f0_measured_envelope_mhz = 12.1230` × 1.5 over a nine-configuration population — four setup axes, each with an interior sample — spanning 0.06 MHz); the structural reflection-zero count, witnessed invariant across the 8.0–8.5-cell iris-thickness ambiguity band by a committed oracle-side sweep; and passband contiguity as a regression lock (span_holes ≤ 1), against the oracle on the as-realized geometry; measured `validation/crossval/_19_iris_filter_results/rfx.json::gated_rfx.d_f0_mhz = 12.08` MHz (positive), 3 zeros versus 3, `validation/crossval/_19_iris_filter_results/rfx.json::gated_rfx.band.span_holes = 1` interior hole bin. A formulation-independent 2-D H-plane FDFD on the same electrical geometry (numpy/scipy only, Richardson over three refinement levels, two-estimate consistency 0.4 MHz — a same-model consistency check, not an error bound; the levels fit convergence order ≈1.25 rather than the assumed 1, worth ~0.7–1.0 MHz of order-assumption uncertainty) agrees with the mode-matching oracle to 1.1 MHz in f0 and 1.0 MHz in bandwidth, and rfx differs from both routes by the same amount — the residual sits on the FDTD side, is mesh-invariant when expressed in cells, and its origin is recorded as unattributed. Band edges and bandwidth are reported, not gated: the iris-thickness leg of the node-plane convention measures ≈(t−0.68)·dx and matches neither integer rule, leaving a half-cell comparator-input uncertainty that moves bandwidth ~40 MHz per cell against f0's ~2.4 MHz. Feeding the oracle the drawn cell counts instead of the realized ones biases f0 by 107.5 MHz — a bias the envelope×1.5 rule cannot catch, because that rule bounds scatter. Worst-case return loss, ripple levels, zero depths, contiguity detail beyond the span_holes lock, the coarse a/60 rung and phase are reported but never gated; ring-down settling, feed clearance and absorber depth are each held to one frequency bin. The built structure is a snapped version of the published filter (one of four structural zeros lost, worst return loss degraded `validation/crossval/_19_iris_filter_results/rfx.json::oracle_nominal_band.worst_rl_db = 13.82 dB` → `validation/crossval/_19_iris_filter_results/rfx.json::oracle_rasterized_band.worst_rl_db = 10.65 dB` by rasterization alone). Says nothing about phase, group delay, loss, fabrication tolerance, arbitrary filters, posts or septa. |
| `crossval/22_dispersive_slab_fresnel.py` | Dispersive single-pole slab (Debye, Lorentz, Drude) normal-incidence `R(f)` and `T(f)` on the cv04 rig with the record length derived per arm from the slab's own ring-down to a −40 dB settling witness (reached `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.run.n_steps = 1108` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.run.n_steps = 1228` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.run.n_steps = 1168` steps), 4–10 GHz | Transfer-matrix calculation with the complex, frequency-dependent ε(f) of the declared pole, gated per bin and band-mean — measured band-mean \|ΔR\| / \|ΔT\|: Debye `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_dR_gated = 0.0023` / `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_dT_gated = 0.0028`, Lorentz `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.mean_dR_gated = 0.0028` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.mean_dT_gated = 0.0016`, Drude `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.mean_dR_gated = 0.00049` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.mean_dT_gated = 0.0017` against the declared means `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_window_R = 0.01` / `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_window_T = 0.017` — and Meep at 40 px/cm with the same material mapped by `crossval/comparators/dispersive_eps.py` (pre-run 1e-9 check `validation/crossval/_22_dispersive_results/meep_lorentz.json::precheck.max_rel_err = 1.8e-15`; the converged rung of a committed 10/20/40 px/cm ladder that measured Meep's own first-order interface term, T orders `validation/crossval/_22_dispersive_results/meep_ladder_summary.json::arms.lorentz.orders.order_dT_10_20 = 0.97` / `validation/crossval/_22_dispersive_results/meep_ladder_summary.json::arms.lorentz.orders.order_dT_20_40 = 0.98`; gated Meep against the transfer matrix — Lorentz `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dR_meep_tmm_gated = 0.005` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dT_meep_tmm_gated = 0.0051`, Drude `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dR_meep_tmm_gated = 0.002` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dT_meep_tmm_gated = 0.0095` — then rfx against Meep — Lorentz `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dR_rfx_meep_gated = 0.004` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dT_rfx_meep_gated = 0.004`, Drude `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dR_rfx_meep_gated = 0.0017` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dT_rfx_meep_gated = 0.0078`). Verdict `validation/crossval/_22_dispersive_results/rfx.json::verdict.exit_code = 0`. Windows are derived, not chosen — round-up(cv04's committed envelope × 1.5) plus the ADE's own temporal-discretization error of ε(f) propagated through the same transfer matrix; the Meep-side windows come from the declared material, never from the parameters the Meep file reports. Pre-declared in `docs/design_notes/20260902_cv22_dispersive_slab_predeclaration.md` with falsifiers that each exited 1 against this passing baseline (τ × 2: `validation/crossval/_22_dispersive_results/rfx__falsifier_debye_tau_x2.json::arms.debye.mean_dT_gated = 0.085` in T; f0 × 1.3: `validation/crossval/_22_dispersive_results/rfx__falsifier_lorentz_f0_x1p3.json::arms.lorentz.mean_dR_gated = 0.093` in R; fp × 1.3: `validation/crossval/_22_dispersive_results/rfx__falsifier_drude_fp_x1p3.json::arms.drude.mean_dT_gated = 0.17` in T; dispersion removed: `validation/crossval/_22_dispersive_results/rfx__falsifier_lorentz_deps_zero.json::arms.lorentz.mean_dT_gated = 0.73` in T; the Meep material mapped with the wrong convention — 2π dropped: Meep vs the transfer matrix `validation/crossval/_22_dispersive_results/rfx__falsifier_meep_lorentz_no_2pi.json::arms.lorentz.meep.mean_dT_meep_tmm_gated = 0.66` in T; γ from δ instead of 2δ: `validation/crossval/_22_dispersive_results/rfx__falsifier_meep_lorentz_gamma_half.json::arms.lorentz.meep.mean_dR_meep_tmm_gated = 0.094` in R), each also checked analytically by `tests/crossval/test_cv22_dispersive_slab_gates.py`, with the unit-level mapping twin in `tests/crossval/test_cv22_dispersive_eps_mapping.py`. Four VESSL rounds, no window moved: round 1 fired on the Lorentz arm at cv04's 719-step record (truncation of the Q = 3 ring-down, diagnosed by pre-declared dx and time-gate ladders in round 2) and Meep's 10 px/cm leg carried its own first-order term (ladder); the note's §11–§14 record every prediction with its miss. Band edges 3–4 and 10–11.9 GHz are reported, not gated. The Meep reference is converged only one rung under the window (Drude Meep-vs-transfer-matrix `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dT_meep_tmm_gated = 0.0095` vs `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_window4_T = 0.017`, 1.8×; 20 px/cm would fail). Scope: normal incidence only, one pole per model, no conductivity-plus-pole mix, 4–10 GHz, one slab thickness and mesh; the documented user path `Simulation.add_material(..., debye_poles=/lorentz_poles=)` (`docs/public/guide/materials-geometry.mdx`) is NOT exercised — the case drives `update_e_debye` / `update_e_lorentz` directly. Says nothing about oblique incidence, multi-pole fits, or mixed Debye+Lorentz cells. Continuum gate + exact-lattice witness at every rung (docs/design_notes/20260903_lattice_witness_standard.md): each of the three committed dx rungs also passes |rfx − exact 1-D Yee lattice| ≤ derived `W_witness` (the budget's float32 term is a statistical estimate, not a bound) (`validation/crossval/_22_dispersive_results/lattice_witness.json::verdict.all_rungs_ok`, `validation/crossval/_22_dispersive_results/lattice_witness.json::verdict.n_rungs = 3`); the lattice model predicts the whole transfer-matrix residual a priori. The Debye arm's lattice gate passes but is declared non-discriminating (its record settles only to 0.0072, so truncation dominates its lattice term). **Recorded consequence (note §5.4): the family's continuum gate is one slab's discretisation error measured inside a window derived from another slab's discretisation error** — re-deriving `W_mean,R` from the non-lattice part of the same measurement (`gate_from_envelope(1.69e-4, quantum=1000)` = 0.001) fails 7 of the 12 committed rungs, and every measured residual equals its own lattice term to two significant figures. No window is changed. Gate test `tests/crossval/test_lattice_witness_gates.py`. |
| `crossval/23_lossy_slab_fresnel.py` | Lossy (conductive) slab — ε' = 4, 10 mm, σ from tan δ 0.1 / 1 / 3 at 7 GHz (0.15577 / 1.5577 / 4.6731 S/m; skin depth 68 / 7.5 / 3.3 mm) — normal-incidence `R(f)`, `T(f)` **and the absorption `A(f) = 1 − R − T`** on the cv22 rig, 4–10 GHz, tand0p1 / tand1 at dx = 1 mm and tand3 at dx/2 (records `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.run.n_steps = 1067` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.run.n_steps = 1158` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.run.n_steps = 2362` steps). Scope: normal incidence only, one thickness, σ-only loss (no pole, no σ + pole mix), 4–10 GHz; rfx has no tan-δ model, so σ is fixed per arm. | Transfer-matrix calculation with the complex ε' − jσ/(ωε0), gated per bin and band-mean on R, T and A — the first crossval case in which dielectric loss is the gated observable. Measured band-mean \|ΔR\| / \|ΔT\| / \|ΔA\|: tand0p1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dR_gated = 0.0039` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dT_gated = 0.0057` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dA_gated = 0.002` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_A = 0.027`, tand1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dR_gated = 0.0051` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dT_gated = 0.0018` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dA_gated = 0.0033` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_A = 0.027`, tand3 `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dR_gated = 0.0031` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dT_gated = 3e-5` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dA_gated = 0.0031` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_A = 0.027` (the T gate on tand3 is VACUOUS, as pre-declared: T_TMM ≤ 0.003 < 0.017 at every gated bin, so its T pass is not a measurement — R and A carry that arm); verdict `validation/crossval/_23_lossy_results/rfx.json::verdict.exit_code = 0`. Two arms (tand1, tand3) build the slab through the documented `Simulation.add_material(..., sigma=)` + `add(Box)` path and assert the assembled arrays equal the direct ones bit-for-bit (`validation/crossval/_23_lossy_results/rfx.json::arms.tand1.materials.api_equals_direct`); the time stepping is the same `update_e` in every arm (`Simulation.run()`'s scan body is not exercised). Windows are derived, not chosen — cv22's cv04-derived ones plus the σ update's own term (σ_eff = σ·x/tan x, x = ωdt/2) — and the A windows are the triangle sums (0.148 per bin, 0.027 band-mean); the closure-derived tighter A window is reported (`validation/crossval/_23_lossy_results/rfx.json::arms.tand3.A_tight_ok`), not gated. The exact 1-D Yee-lattice solution of the staircase slab is carried as a reported witness and reproduces every arm bin by bin (|rfx − lattice| `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.lattice.mean_dR_lattice_gated = 3e-5` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.lattice.mean_dR_lattice_gated = 2.1e-5` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.lattice.mean_dR_lattice_gated = 3e-5` in R): the rfx-vs-transfer-matrix residual is the lattice's own second-order term, which is why tand3 (|n| up to 4.6) runs at dx/2 — resolution, not tolerance; carrying it as a window term is the alternative put to the PI. Meep with `D_conductivity = σ·a/(c ε0 ε')` (mapping unit-tested to 1e-9 with wrong-scaling falsifiers in `tests/crossval/test_cv23_lossy_eps_mapping.py`, pre-checked on the pod through `Medium.epsilon(f)`: `validation/crossval/_23_lossy_results/meep_tand1.json::precheck.max_rel_err = 1.6e-16`) at `validation/crossval/_23_lossy_results/meep_tand0p1.json::resolution = 80` px/cm on tand0p1 and `validation/crossval/_23_lossy_results/meep_tand3.json::resolution = 40` px/cm on tand1 / tand3 is gated against the transfer matrix and rfx against Meep on R, T and A — tand0p1 Meep-vs-TMM `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_dR_meep_tmm_gated = 0.0078` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_window4_R = 0.01` (1.3×, one rung under the window and a known +a/80 thickness systematic in the reference — Meep's `Block` without `eps_averaging` realizes d + a/res, Meep minus TMM(d + a/80) is 8.8e-5 in the mean; the 40 px/cm rung `validation/crossval/_23_lossy_results/meep_ladder_summary.json::arms.tand0p1.rungs.40.mean_dR_meep_tmm_gated = 0.016` would fail), tand3 `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.meep.mean_dR_meep_tmm_gated = 0.00091`; rfx-vs-Meep tand0p1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_dR_rfx_meep_gated = 0.0052`, tand1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.meep.mean_dR_rfx_meep_gated = 0.0045`, tand3 `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.meep.mean_dR_rfx_meep_gated = 0.0022`. Committed 10/20/40/80 px/cm ladders: tand0p1 first order (`validation/crossval/_23_lossy_results/meep_ladder_summary.json::arms.tand0p1.orders.order_dR_20_40 = 1`), tand3 second order (`validation/crossval/_23_lossy_results/meep_ladder_summary.json::arms.tand3.orders.order_dR_20_40 = 1.9`); the first-order term is a one-cell thickness excess of Meep's `Block` without `eps_averaging` (nominal block `validation/crossval/_23_lossy_results/meep_ladder_summary.json::arms.tand0p1.rungs.40.mean_dR_meep_tmm_gated = 0.016`; centre shifted half a pixel `validation/crossval/_23_lossy_results/meep_ladder_summary.json::diagnostics.tand0p1.res40_shift_half.mean_dR_meep_tmm_gated = 0.00024`). Pre-declared in `docs/design_notes/20260902_cv23_lossy_slab_predeclaration.md` with falsifiers that each exited 1 against this passing baseline (σ × 1.5: tand3 `validation/crossval/_23_lossy_results/rfx__falsifier_tand3_sigma_x1p5.json::arms.tand3.mean_dR_gated = 0.085` in R; σ = 0: tand1 `validation/crossval/_23_lossy_results/rfx__falsifier_tand1_sigma_zero.json::arms.tand1.mean_dA_gated = 0.74` in A; the gain arm σ → −σ with `validation/crossval/_23_lossy_results/rfx__falsifier_tand0p1_sigma_neg.json::arms.tand0p1.gates.G3_passivity` false; Meep σ_D × 2π: `validation/crossval/_23_lossy_results/rfx__falsifier_meep_tand1_sigma_2pi.json::arms.tand1.meep.mean_dR_meep_tmm_gated = 0.35` in R, pre-check `validation/crossval/_23_lossy_results/meep_tand1__falsifier_sigma_2pi.json::precheck.max_rel_err = 4.4`). Three VESSL rounds, no physics window or gate moved: round 1 fired tand3 at dx and tand0p1's Meep at 40 px/cm; the lattice solution and the dx ladder decided both. One diagnostic assertion was relaxed after the measurement — the pod gate test exited 1 in all three rounds on the tail-fit block count (nb ≥ 3 → nb ≥ 2, flagged `fit_reliable`), and the tail-fit keys of the 16 committed `rfx*.json` were post-processed, no rerun (note §15). Band edges 3–4 and 10–11.9 GHz are reported, not gated. Gate test `tests/crossval/test_cv23_lossy_slab_gates.py`. Continuum gate + exact-lattice witness at every rung (docs/design_notes/20260903_lattice_witness_standard.md): all nine committed dx rungs also pass |rfx − exact 1-D Yee lattice| ≤ derived `W_witness`, per bin and band-mean (the budget's float32 term is a statistical estimate, not a bound) (`validation/crossval/_23_lossy_results/lattice_witness.json::verdict.all_rungs_ok`, `validation/crossval/_23_lossy_results/lattice_witness.json::verdict.n_rungs = 9`, worst per-bin ratio 0.30) — round 3's reported witness is now a gate at every rung. **Recorded consequence (note §5.4): the family's continuum gate is one slab's discretisation error measured inside a window derived from another slab's discretisation error** — re-deriving `W_mean,R` from the non-lattice part of the same measurement (`gate_from_envelope(1.69e-4, quantum=1000)` = 0.001) fails 7 of the 12 committed rungs, and every measured residual equals its own lattice term to two significant figures. No window is changed. Gate test `tests/crossval/test_lattice_witness_gates.py`. |
| `crossval/20_msl_phase_referee.py` | MSL de-embedded phase (`angle(S21)`) vs an external OpenEMS referee, matched-mesh (`dx=50um`) RO4350B thru line | OpenEMS `MSL_NotchFilter.py` tutorial reproduce-gate plus a matched-mesh thru-line comparison against rfx's own `compute_msl_s_matrix`; resolves whether rfx's previously un-gated MSL phase was a time-convention or reference-plane mismatch (both tools' DFT kernels are identical at the source — it was reference-plane, resolved by matching the measurement plane to rfx's own probe-0 coordinate). Gated: each solver's own `angle(S21)` against its own measured `beta` (intra-run self-consistency, 3.0°/200 ps) over 3.0–4.5 GHz; each solver's measured `beta` against the Hammerstad–Jensen quasi-TEM closed form of the realized board (2.0%; measured 0.94% rfx, 0.31% openEMS); and — since 2026-09-01, issue #812 — the **raw** cross-solver `angle(S21)` difference itself (3.0°; measured 0.342°), which was previously reported only. The self-consistency leg alone cannot fail on a coherent phase-velocity error: a factor-2 error reads `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.blindness.audit_construction_e1_max_phase_dev_deg = 0.241` degrees against its 3° gate. The 500 MHz bin is excluded — the line is ~0.03 guide wavelengths there, where port extraction is ill-conditioned. Reported, not gated: the dispersion-corrected residual, which is provably blind to the same error class. The `CalcPort` reference-plane-referral rotation is unexercised on this fixture and untested off-grid. Applies to one geometry, one mesh, single drive. |
| `crossval/21_coax_two_port_referee.py` | Coax two-port through-line vs an external OpenEMS referee, matched-mesh (`dx=0.37474mm`) SMA-class PTFE coax (`a=0.635mm`, `b=2.055mm`, `L12=58.4595mm`) | OpenEMS `Coax.m` tutorial reproduce-gate (Stage A, against Coax.m's own closed-form line impedance) plus a matched-mesh `CoaxialPort` thru-line comparison against rfx's own `compute_coaxial_two_port` fixture (Stage B, two drives, full S-matrix) | Comparator-leg referee — brackets, does not judge rfx's own numbers; `compute_coaxial_two_port` itself remains EXPERIMENTAL. Gated: Stage A reproduces Coax.m's own ZL check (50.432 ohm vs analytic 49.940 ohm); Stage B gates each solver's own self-consistency — passivity, a matched-through magnitude band (gated central-band leg measured 0.9999966–1.0000282), a phase/group-delay witness against the port's own measured `beta` (intra-run self-consistency; under it, phase deviation collapses from 111.38° to <0.44°), and — since 2026-09-01, issue #812 — the port's measured `beta` and the group delay measured from `S21` alone against the **exact continuum coax TEM** values, inside a mesh-dependent staircase envelope derived from the committed refinement law (0.157 at the registered mesh, 0.086 at the 1.5× refinement; measured 0.121 and 0.066). The self-consistency leg alone cannot fail on a coherent phase-velocity error: it reads the same deviation at every perturbation factor measured, 0.50 through 1.57. The new analytic leg has a pre-declared **two-sided** detection floor and is itself blind between `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.predeclared_k_lo = 0.752106` and `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.predeclared_k_hi = 1.032334` at the registered mesh — no analytic gate on a 3.8-cell annulus can be tighter than that annulus's own staircase bias; the floor as declared is the 1.30 headroom's choice, though — headroom 1.10 would put it at `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.headroom_dependence[1].k_hi = 1.010778` with criterion (A) intact, and the physics-forced floor at this mesh is `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.physics_forced_registered.k_hi = 1.003945` (round-2 review, 2026-09-02; the headroom is not tightened post hoc). Refining the mesh moves the floor with no change to the gate. (The ~1.12× gap between measured and analytic `beta` is staircase dispersion on this fixture's coarse PTFE annulus, now measured by a mesh-refinement convergence witness — issue #489 leg 1, VESSL 369367251845: a 1.5x refinement moves the ratio 1.1208→1.0662 (implied order p≈1.5), pre-declared one-sided acceptance CONFIRMED. The four agreeing `beta` readings at the original mesh density are one witness, not four — same run, same solver, same extractor; the mesh-refinement run is the independent second axis). Reported, not gated: run-3's own `\|S21\|`~=1.000 across 4–12 GHz for this independent, differently-discretized geometry, and reciprocity (`\|S21\|` vs `\|S12\|`, essentially exact). Applies to one geometry, one mesh, two drives (forward + reverse). |

When reporting a result, include the script path, commit, geometry, mesh,
frequency range, comparator, and numerical thresholds. Do not generalize a
successful case to a different port, material model, boundary, or mesh without
an applicable comparison.

## Other validation directories

- `research/subgrid/12_subgrid_disjoint_prototype.py` is a short-duration
  research check for a topology that is not long-time energy-stable.
- `research/subgrid/13_subgrid_material_validation.py` demonstrates the guarded
  subgrid boundary and checks the same material setup on a uniform mesh.
  SBP-SAT/subgridding is not a supported feature.
- `tmtt_paper/` contains paper-support scripts. Values identified there as
  targets are experiment goals, not measured results. See
  `tmtt_paper/README.md` for the status of each script.
