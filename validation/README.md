# rfx Validation Scripts

These scripts compare rfx results with analytic calculations, other solvers,
or a second rfx configuration. They are test cases, not tutorials. Start with
`examples/README.md` if you are learning the software.

Each comparison covers the geometry, mesh, frequency range, source, and output
implemented by that script. A passing result does not establish accuracy for a
different setup.

## Run a validation case

Run scripts from the repository root, one case at a time:

```bash
python validation/crossval/11_waveguide_port_wr90.py
```

The process exit code has a specific meaning:

- `0`: every configured numerical check passed, including a required external
  comparison when the case has one.
- `1`: a simulation, numerical check, or required execution step failed.
- `2`: the rfx checks completed, but a required reference file or external
  solver was unavailable. This result is inconclusive, not a pass.

See `crossval/manifest.json` for each script's dependencies, expected exit
codes, numerical-check files, and generated artifacts.

## Available cases

| Script | What it checks | Reference and interpretation limit |
| --- | --- | --- |
| `crossval/06b_msl_notch_filter_uniform.py` | Uniform-mesh microstrip-notch response | The script checks a quarter-wave estimate at dx=63.5 µm = h_sub/4 (issue #723; it shipped at dx = 80 µm through 2026-08, where the declared 254 µm substrate rasterized to 320 µm — a **pre-2.0** realization. From 2.0 (#931) a conductor is declared a volume, a sheet or a wire and drawn extent equals realized extent, so the same drawing on the same mesh no longer produces that 320 µm board; what remains is that 254 µm is not an integer number of 80 µm cells, which is why the case ships at dx = 63.5 µm = h_sub/4 — the references described a board that mesh did not solve). The analytic reference is evaluated on the realized 635.0 µm trace width, read live from `sim.fidelity_report()`. (Under the contract a sheet footprint is sampled **closed** on the in-plane axes — geometric node span 571.5 µm, electrical width n_rows·dx 635.0 µm — so the realized width is re-read from the recomputed run's own `fidelity_report()` rather than carried over.) A separate dx=50 µm OpenEMS result is checked by `tests/crossval/test_msl_notch_e4_comparison_gates.py`, and after #723 that comparison is on a DIFFERENT board — its rfx leg still realizes h_sub=300 µm. The shipped run is not an external-solver comparison. Neither establishes general MSL-port accuracy. The current run is the post-#931 re-solve (VESSL 369367259191, 2026-09-07, trace and stub declared as sheets; log `validation/crossval/_06b_notch_uniform_logs/20260907T124851Z_run.log`): frequency error 2.16 %, notch depth −39.4 dB, median Re(Z0) 48.2 Ω, width ratio 0.9991, half-grid witness 0.4469 bin — all four gates pass. The same board realized as one-cell PEC Boxes read 1.40 % / −43.3 dB / 46.5 Ω, which is pre-2.0 history. The width convention is not settled by that run: the pre-declared Z0 falsifier (46.48 ± 1.0 Ω for the electrical 635.0 µm against ≈49.4 Ω for the geometric 571.5 µm) landed between the two at 48.2 Ω; both readings are rejected and nothing is re-derived from one board at one dx (crossval-B, fae08d10, which also committed the regenerated falsifier summary — the two citations that follow resolve against it). Re-gated 2026-09-01 (issue #812) and judged on its own board 2026-09-02 (pre-2.0 realization: all gates passed at 1.453 %, and a one-cell stub-length falsifier FIRED at a refined shift of 0.145 % of a predicted 0.532 %, so sub-bin notch resolution was not demonstrated there). On the sheet board the same summary reads `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::criterion_A_baseline.err_pct = 2.1649` % (all four gates pass) and the same one-cell stub arm reads `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::stub_1cell.refined_delta_pct = 0.823` % against the 0.532 % closed form — over-response (1.55x) where there was under-response (0.27x), `stub_1cell.visible` false → true, so criterion (B)'s visibility test closes; neither response is attributed and visibility is not an accuracy statement. |
| `crossval/07_sheen_lpf.py` | Sheen 1990 stepped-impedance microstrip low-pass filter stopband | Characterization study, not an accuracy comparison. A committed Palace FEM referee shows the stopband is a double transmission zero (~7.0 and ~8.0 GHz) that OpenEMS reproduces to about 0.7%. The original rfx leg (num_periods=20, default probe offset) was non-passive (58/120 bins, worst column power 1794.5) and its argmin sat 9.6% from OpenEMS; the root cause — record truncation (settling witness −24.7 dB) plus probe near-field contamination — was measured and the leg regenerated 2026-07-26 (num_periods=60, witness −72 dB; n_probe_offset=30, which satisfies both the 5·h_sub near-field and the λ_g/4 reflector clearances; passivity-enforced `\|S\|` strictly ≤ 1 with the raw excess recorded per bin). The current #931 sheet leg and derived referee were regenerated by VESSL 369367259192 (2026-09-07, commit 069bdebc): raw-bin rfx argmin 8.202 GHz vs OpenEMS 7.983 GHz (2.74% split), parabolic FDTD argmins 8.244069 / 7.994749 GHz. Both doublet members remain resolved (structure distance 2.87% vs OpenEMS 0.66%; sides_with = openems stands); the separately windowed argmin distance to Palace is 2.3756%, not the 2.8668% structure distance. Read narrowly still: `passivity_correction` > 0.05 occurs in 3/120 bins on the current leg (3/120, worst 0.6572 at 17.870 GHz), all three above 17 GHz and none inside the 5–15 GHz null band. This is the case's characterized high-frequency residual on a coarser realized strip at the same dx = 200 µm, not evidence of a physics regression in the stated null band; it is not a magnitude-accuracy claim — the pre-#516 leg's 84/120 bins, worst 0.365, was substantially extractor defects, not a coarse-mesh envelope — and the fitted passband `Re(Z₀)` reads 51.9 Ω vs OpenEMS's flat ~51 Ω (printed, not gated; the old ~67 Ω fit-conditioning residual is gone; the in-band 5–15 GHz median is 54.7 Ω); the null position is insensitive to this. The checks lock the OpenEMS reference's soundness, the comparison shape, the committed null frequencies, the rfx passband mean, the referee verdict, and the evidence chain (strict bound, settling witness, correction footprint; fail-closed if a leg lacks the fields). Re-gated 2026-09-01 (issue #812 mechanism P3, `docs/design_notes/estimator_resolution_regate.md`): the committed null was a bin argmin on a 163.866 MHz grid (2.081%/bin) judged against a declared 1% window, so erasing the lower doublet member left every gated number bit-identical and passed 17/17. The argmin lock is unchanged; added are sub-bin log-parabolic locks on both doublet members for both solvers (`±0.50%`, against the committed referee fixture's own `fdtd_doublet_ghz`), a transmission-zero COUNT gate (`== 2` at `≤ −20 dB`), a `−3 dB` corner-frequency lock (`±0.25%`) on a quantity the case never gated, and an in-run half-grid resolution witness. The #931 regeneration records 28/28 passing on the committed legs. The erased-zero and corner-error falsifiers failed exactly their intended gates on the pre-#931 leg (`tests/fixtures/cv07_estimator_regate/cv07_estimator_falsifiers.json`); that calibration and the tool's fixed anchors have not been regenerated for #931 and are historical evidence. |
| `crossval/11_waveguide_port_wr90.py` | WR-90 empty guide, PEC short, and dielectric slab | Analytic checks determine the exit status; optional Meep, OpenEMS, and Palace columns are diagnostic. The broad-E4 artifact is refreshed from current-main VESSL run 369367260736 (producer commit 8206031d) after aligning the 10 mm dielectric Box to explicit lattice faces. Five Palace pairs pass unchanged `0.10`/`0.07` tolerances; slab S11 max/mean are `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::pairs[3].max_mag_abs_diff = 0.0193` / `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::pairs[3].mean_mag_abs_diff = 0.007767`, slab S21 max is `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::pairs[4].max_mag_abs_diff = 0.0044`, and the aggregate max is `tests/fixtures/waveguide_broad_e5/wr90_rectangular_broad_e4_comparison.json::summary.max_mag_abs_diff = 0.0193`. The historical 0.0707 record and the pre-fix 0.1500 result, plus the 11-node realization A/B, remain in `docs/design_notes/20260831_cv11_broad_e4_artifact_provenance.md`; no phase or general junction/nonuniform accuracy claim follows. |
| `crossval/15_patch_antenna_rt5880.py` | Probe-fed patch on a 3.175 mm RT/Duroid 5880 laminate, uniform mesh | A same-geometry rfx-versus-OpenEMS integration study on a different laminate from the former cv05 patch case, removed 2026-09-21, which it does not replace. It checks a coarse-mesh envelope (resonance within 8% of the OpenEMS dip, a −40 dB settling witness, passivity on both sides, broadside directivity within 3 dB) and asserts the substrate rasterizes to exactly four cells before quoting a resonance. Issue #812 (round 2): its 8 % f0 gate is blind to the #740 one-plane-ground realization (`validation/crossval/_15_patch_results/rfx_one_plane_ground_b29f9de7.json::f_harminv_hz = 2471858108` Hz vs `validation/crossval/_15_patch_results/openems.json::f_dip_hz = 2330000000` Hz, +6.09 %); only the build assertion catches it, and no ratio-band window with provenance could be derived — recorded as an honest STOP, gates unchanged. **PRE-2.0 (#931).** The "#740 one-plane-ground realization" that sentence names no longer exists: from 2.0 a conductor is a volume, a sheet or a wire by declaration, a one-cell PEC Box realizes walls at both drawn faces, and cv15's ground is declared as a sheet on the substrate floor. The cited `rfx_one_plane_ground_b29f9de7.json` figure stays as the dated record of what the old realization cost; it is not reproducible on the current tree, and the case's negative control is now a wrong DECLARED ground plane rather than a realization flag. The 8 % f0 gate's blindness is a property of the gate and is unchanged by any of that. Patch accuracy is not established here; patch evidence remains with the committed tests the former cv05 patch case named (regression locks, not accuracy claims). |
| `crossval/18_wr90_iris_modematch.py` | Single symmetric inductive iris in WR-90 vs a TEn0 mode-matching oracle, 8.2–12.4 GHz | First calibrated PEC-obstacle evidence in the rectangular-waveguide lane (multi-iris filters stay experimental — this is the prerequisite stage). Gated (re-derived 2026-09-01, issue #812): fine rung dx = a/60 within a per-configuration window (round-up of each row's own envelope × 1.5, from `validation/crossval/_18_wr90_iris_results/aperture_resolution.json::pairs[2].fine_gate_abs = 0.006` to `validation/crossval/_18_wr90_iris_results/aperture_resolution.json::pairs[4].fine_gate_abs = 0.016` abs; the pooled 0.02 abs is kept as an outer bound) across eight configurations, so a one-cell aperture error is detected in BOTH signs at every configuration (committed detection table in `validation/crossval/_18_wr90_iris_results/aperture_resolution.json`), plus a Richardson-extrapolation cross-check on the oracle within 0.01 abs at every fine/coarse pair; the first-order gap ratios (0.407–0.440) are the attribution witness. Coarse rung, raw extraction and residual ripple (measured against the oracle: fine ≤ 0.0076, coarse ≤ 0.0152) are reported but never gated. Three setup defects (parasitic wall-slot, half-ulp node-plane corners, an electrical aperture of d + 2·dx together with a too-thin absorber — the last of these is a **pre-2.0** defect: under the lattice ownership contract a PEC volume realizes walls at both drawn faces, so the electrical aperture is the drawn `d` and the `+ 2·dx` fence is retired rather than re-tuned) were found and are each fenced; the earlier modal-extraction fence is retracted because its non-passivity evidence does not survive the corrected setup, with modal accuracy committed alongside (a little worse than flux, which keeps the gate). **#931 pass 2 has run and landed**, so every gate number in this row is the post-contract one and the citations above resolve against the regenerated `aperture_resolution.json`. Pass 1 (VESSL 369367259159) re-solved the migrated geometry with the old constants and exited non-zero by design — the `--write-fixture` self-check demands each constant equal round-UP(its own envelope × 1.5). Pass 2 (VESSL 369367259291, rc 0, 2026-09-07) is the identical command after the constants were re-derived by that same rule, per configuration at the case's quantum, from pass 1's own envelopes; that arithmetic is the only thing that moved a number here, and all ten constants moved down or stayed. No window is widened by hand, and a re-derived window that comes out wider ships with the envelope and the arithmetic that produced it. Says nothing about phase, multi-iris filters, posts, septa, or off-centre apertures. |
| `crossval/19_wr90_iris_filter_aghanim.py` | A published 4th-order WR-90 inductive-iris bandpass filter (five irises, four coupled cavities) vs a TEn0 mode-matching cascade oracle, 10.40–11.70 GHz | Diagnostic only — it reports a comparison and is not evidence for broader support. The reference HFSS/CST numbers are **digitized from the paper's figure**; no external solver is run here, and rfx is not compared against either commercial code. The first resonant multi-obstacle case in the rectangular-waveguide lane, where a per-face geometry error is a passband shift rather than a magnitude tolerance. Gated: centre frequency within `validation/crossval/_19_iris_filter_results/rfx.json::gates.f0_gate_mhz = 19.0` MHz (envelope `validation/crossval/_19_iris_filter_results/rfx.json::gates.f0_measured_envelope_mhz = 12.1219` × 1.5 over a nine-configuration population — four setup axes, each with an interior sample — spanning 0.02 MHz); the structural reflection-zero count, witnessed invariant across the 8.0–8.5-cell iris-thickness ambiguity band by a committed oracle-side sweep; and passband contiguity as a regression lock (span_holes ≤ 1), against the oracle on the as-realized geometry; measured `validation/crossval/_19_iris_filter_results/rfx.json::gated_rfx.d_f0_mhz = 12.12` MHz (positive), 3 zeros versus 3, `validation/crossval/_19_iris_filter_results/rfx.json::gated_rfx.band.span_holes = 1` interior hole bin. A formulation-independent 2-D H-plane FDFD on the same electrical geometry (numpy/scipy only, Richardson over three refinement levels, two-estimate consistency 0.4 MHz — a same-model consistency check, not an error bound; the levels fit convergence order ≈1.25 rather than the assumed 1, worth ~0.7–1.0 MHz of order-assumption uncertainty) agrees with the mode-matching oracle to 1.1 MHz in f0 and 1.0 MHz in bandwidth, and rfx differs from both routes by the same amount — the residual sits on the FDTD side, is mesh-invariant when expressed in cells, and its origin is recorded as unattributed. **#931:** regenerated under the lattice ownership contract. The drawn cell counts are the plain roundings now — the case carried a +1 / −1 compensation that existed only to cancel a realization in which a body's far face was never a wall — and the built filter is unchanged by the deletion (same realized wall planes, same cavities, same apertures); the 8.0–8.5-cell iris-thickness "ambiguity band" is re-centred on the realized thickness. The ≈(t−0.68)·dx iris-thickness fit is **superseded**: it was the signature of a far face the pre-2.0 realization never gave a drawn volume, not a convention to choose between, and the oracle is fed the drawn thickness. Band edges and bandwidth stay reported, not gated, and pass 2 settled why. Their envelopes now exist and are committed (edges 17.0553 MHz, bandwidth 9.9024 MHz over the nine-configuration population), together with the gates they would give, 26.0 and 15.0 MHz, marked `applied: false`. They are not applied because that population is single-mesh — every member is a/90 — while lattice rounding is the dominant term for these two observables at 22–40 MHz per cell against f0's ~2.4 MHz; the a/60 diagnostic rung reads +24.51 / +15.24 MHz on them. A 1.5× lock over a population blind to the dominant term pins the mesh choice rather than bounding the solver, so re-gating needs its own pre-declaration and a cross-mesh sensitivity measurement. Before #931, feeding the oracle the drawn cell counts instead of the realized ones biased f0 by 107.5 MHz — historical evidence that the envelope×1.5 rule bounds scatter, not bias. The current drawn and realized counts coincide. Worst-case return loss, ripple levels, zero depths, contiguity detail beyond the span_holes lock, the coarse a/60 rung and phase are reported but never gated; ring-down settling, feed clearance and absorber depth are each held to one frequency bin. The built structure is a snapped version of the published filter (one of four structural zeros lost, worst return loss degraded `validation/crossval/_19_iris_filter_results/rfx.json::oracle_nominal_band.worst_rl_db = 13.82 dB` → `validation/crossval/_19_iris_filter_results/rfx.json::oracle_rasterized_band.worst_rl_db = 10.65 dB` by rasterization alone). **#931 pass 2 has run and landed: the f0 gate, the envelope and the measured `d_f0` cited above are the post-contract record.** The pre-declared falsifier was that `d_f0` stays at +12.08 MHz; pass 1 (VESSL 369367259160) measured +12.12 and pass 2 (VESSL 369367259297, rc 0, 2026-09-07) reproduced it, a 0.04 MHz move where a refutation would have been of order 100 MHz. The gate did not move: the re-derived `ceil(12.1219 × 1.5)` is 19.0, the value already in the source. That rule is the only arithmetic that moved a number in this row, and no window is widened by hand. Says nothing about phase, group delay, loss, fabrication tolerance, arbitrary filters, posts or septa. |
| `crossval/20_msl_phase_referee.py` | MSL de-embedded phase (`angle(S21)`) vs an external OpenEMS referee, matched-mesh (`dx=50um`) RO4350B thru line | OpenEMS `MSL_NotchFilter.py` tutorial reproduce-gate plus a matched-mesh thru-line comparison against rfx's own `compute_msl_s_matrix`; resolves whether rfx's previously un-gated MSL phase was a time-convention or reference-plane mismatch (both tools' DFT kernels are identical at the source — it was reference-plane, resolved by matching the measurement plane to rfx's own probe-0 coordinate). Gated: each solver's own `angle(S21)` against its own measured `beta` (intra-run self-consistency, 3.0°/200 ps) over 3.0–4.5 GHz; each solver's measured `beta` against the Hammerstad–Jensen quasi-TEM closed form of the realized board (2.0%; measured 0.94% rfx, 0.31% openEMS — pre-2.0 realization; re-measured under #931); and — since 2026-09-01, issue #812 — the **raw** cross-solver `angle(S21)` difference itself (3.0°; historical pre-#931 run-2 pair measured 0.342°), which was previously reported only. The committed replay with the current rfx leg and historical run-2 OpenEMS leg reads 0.5308° raw phase difference and analytic-beta errors of 1.4122% rfx / 0.3068% OpenEMS (`validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.run2_openems_with_current_rfx_fixture.cross_solver_max_abs_raw_phase_diff_deg = 0.5308`); this is not a fresh matched-board OpenEMS solve after #931, which remains outstanding. The self-consistency leg alone cannot fail on a coherent phase-velocity error: a factor-2 error reads `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.blindness.audit_construction_e1_max_phase_dev_deg = 0.0647` degrees against its 3° gate. The 500 MHz bin is excluded — the line is ~0.03 guide wavelengths there, where port extraction is ill-conditioned. Reported, not gated: the dispersion-corrected residual, which is provably blind to the same error class. The `CalcPort` reference-plane-referral rotation is unexercised on this fixture and untested off-grid. Applies to one geometry, one mesh, single drive. |
| `crossval/21_coax_two_port_referee.py` | Coax two-port through-line vs an external OpenEMS referee, matched-mesh (`dx=0.37474mm`) SMA-class PTFE coax (`a=0.635mm`, `b=2.055mm`, `L12=58.4595mm`) | OpenEMS `Coax.m` tutorial reproduce-gate (Stage A, against Coax.m's own closed-form line impedance) plus a matched-mesh `CoaxialPort` thru-line comparison against rfx's own `compute_coaxial_two_port` fixture (Stage B, two drives, full S-matrix) | Comparator-leg referee — brackets, does not judge rfx's own numbers; `compute_coaxial_two_port` itself remains EXPERIMENTAL. Gated: Stage A reproduces Coax.m's own ZL check (50.432 ohm vs analytic 49.940 ohm); Stage B gates each solver's own self-consistency — passivity, a matched-through magnitude band (gated central-band leg measured 0.9999966–1.0000282), a phase/group-delay witness against the port's own measured `beta` (intra-run self-consistency; under it, phase deviation collapses from 111.38° to <0.44°), and — since 2026-09-01, issue #812 — the port's measured `beta` and the group delay measured from `S21` alone against the **exact continuum coax TEM** values, inside a mesh-dependent staircase envelope derived from the committed refinement law (0.157 at the registered mesh, 0.086 at the 1.5× refinement; measured 0.121 and 0.066). The self-consistency leg alone cannot fail on a coherent phase-velocity error: it reads the same deviation at every perturbation factor measured, 0.50 through 1.57. The new analytic leg has a pre-declared **two-sided** detection floor and is itself blind between `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.predeclared_k_lo = 0.752106` and `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.predeclared_k_hi = 1.032334` at the registered mesh — no analytic gate on a 3.8-cell annulus can be tighter than that annulus's own staircase bias; the floor as declared is the 1.30 headroom's choice, though — headroom 1.10 would put it at `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.headroom_dependence[1].k_hi = 1.010778` with criterion (A) intact, and the physics-forced floor at this mesh is `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv21.detection_floor.physics_forced_registered.k_hi = 1.003945` (round-2 review, 2026-09-02; the headroom is not tightened post hoc). Refining the mesh moves the floor with no change to the gate. (The ~1.12× gap between measured and analytic `beta` is staircase dispersion on this fixture's coarse PTFE annulus, now measured by a mesh-refinement convergence witness — issue #489 leg 1, VESSL 369367251845: a 1.5x refinement moves the ratio 1.1208→1.0662 (implied order p≈1.5), pre-declared one-sided acceptance CONFIRMED. The four agreeing `beta` readings at the original mesh density are one witness, not four — same run, same solver, same extractor; the mesh-refinement run is the independent second axis). Reported, not gated: run-3's own `\|S21\|`~=1.000 across 4–12 GHz for this independent, differently-discretized geometry, and reciprocity (`\|S21\|` vs `\|S12\|`, essentially exact). Applies to one geometry, one mesh, two drives (forward + reverse). |

When reporting a result, include the script path, commit, geometry, mesh,
frequency range, comparator, and numerical thresholds. Do not generalize a
successful case to a different port, material model, boundary, or mesh without
an applicable comparison.

## Other validation directories

- `research/subgrid/12_subgrid_disjoint_prototype.py` is a short-duration
  research check for a topology that is not long-time energy-stable.
- `research/subgrid/13_subgrid_material_validation.py` demonstrates the guarded
  subgrid boundary and checks the same material setup on a uniform mesh.
  SBP-SAT/subgridding is not a supported feature.
- `tmtt_paper/` contains paper-support scripts. Values identified there as
  targets are experiment goals, not measured results. See
  `tmtt_paper/README.md` for the status of each script.
