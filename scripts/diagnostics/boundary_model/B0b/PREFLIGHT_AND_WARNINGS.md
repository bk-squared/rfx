# Verbatim execution/preflight/warning records

## full_theta0_r1

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta15_r1

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta30_r1

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta45_r1

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta0_r2

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta30_r2

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta0_r4

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_theta30_r4

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## full_forward_theta30_r1

```text
  [PREFLIGHT] Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [forward] preflight found 2 advisory issue(s) - pass skip_preflight=True to suppress:
  - Material 'slab' (geometry entry #0, Box) extends into CPML region along x-axis: bbox hi face at 8.432mm is 936.9µm past the x-hi absorber boundary at 7.495mm. CPML modifies field updates — geometry inside the absorber is physically meaningless (issue #61).
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## ris_theta30_r1

```text
  [PREFLIGHT] all dielectric(s) ['substrate'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [run] preflight found 1 advisory issue(s) - pass skip_preflight=True to suppress:
  - all dielectric(s) ['substrate'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## tfsf_theta30_r1

```text
  [PREFLIGHT] All checks passed.
  [PREFLIGHT] all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
WARNING UserWarning: [forward] preflight found 1 advisory issue(s) - pass skip_preflight=True to suppress:
  - all dielectric(s) ['slab'] are perfectly lossless in an open (CPML) domain. If you are measuring Q / resonance, this gives an ARTIFICIALLY infinite Q (design-guide Anti-Pattern #1, an R5 surface-metric trap) — add loss, e.g. sigma = 2*pi*f*eps0*eps_r*tan_delta. (Harmless if you are not measuring Q.)
```

## explicit_sparams_theta30_r1

```text

```

```text
ValueError: run(compute_s_params=True) computes Result.s_params only for add_port(...) lumped or wire ports; add_floquet_port(...) is experimental and has no claims-bearing run(compute_s_params=True) S-matrix path.
```

