# The raw trace archive is ~9 MB and is regenerated by
# scripts/diagnostics/issue1041_v2_step_order.py --out <this dir>.
# The committed summary.json carries every number the #1041 verdict cites.
*.npz
