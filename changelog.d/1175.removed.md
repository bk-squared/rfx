### Removed — the MSL thru-line phase cross-validation case (#1175)

- `validation/crossval/20_msl_phase_referee.py` (the RO4350B microstrip thru
  line's `angle(S21)` against openEMS), its run records, its header test and
  its two job files are gone, as the cross-validation redesign note
  (`docs/design_notes/20260921_crossval_role_redesign.md`) decided. The frozen
  rfx record `tests/fixtures/msl_phase_referee/msl_thru_rfx_dx50.json` and the
  diagnostics that read it stay.
- Nothing loads the case any more. The probe-fed microstrip referee's openEMS
  notch-tutorial leg runs on `tests/crossval/_openems_tutorial_gate.py`, and the
  dx ladder's finite-thickness correction lives in the ladder; both give the same
  numbers as before. The tutorial's recorded reproduction log moved to
  `tests/crossval/msl_notch_filter/reference/`. Everything removed is at commit
  `b25df603`.
