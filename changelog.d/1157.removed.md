### Removed — four closed-form cross-validation cases: `03_straight_waveguide_flux`, `09_half_symmetric_waveguide`, `14_rect_cavity_pozar`, `16_pec_sphere_mie_ka_sweep` (#1157)

- A structure with an exact closed form is compared with that closed form by the
  analytic tests under `tests/oracle/`, not with another solver
  (`docs/design_notes/20260921_crossval_role_redesign.md`). What still runs:
  the PEC–PMC quarter-wave ladder (`test_boundary_pmc_oracle.py`), the TM110
  cavity (`test_cavity.py`), the PEC sphere against the Mie series
  (`test_rcs_mie_fixture.py`), and the guide that runs into the absorber
  (`tests/unit/boundaries/test_boundary_touching_dielectric_pad_continuation.py`).
- No longer compared with a closed form by anything: the effective index of a
  dielectric slab guide, cavity modes beyond TM110, and the sphere's ka sweep.
- Four docstrings under `rfx/` (`run_until_decay`, `Simulation.run`, the
  convergence-sweep factory, the openEMS emitter's PMC note) no longer point at
  a removed script. No behaviour changes.
- Everything removed is at commit `b0f2ff9d`.
