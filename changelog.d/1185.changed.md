### Changed — distributed entries require integer exchange_interval=1 (#1185)

- Public `run()` and both distributed runners raise `ValueError` for other exchange intervals; use `exchange_interval=1`.
