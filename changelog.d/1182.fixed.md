### Fixed — the paper's microstrip stub-notch example roots its stub at the edge the trace sheet realizes, so the stub is connected to the line again (#1182)

- `validation/tmtt_paper/msl_stub_notch_tuning.py` rooted both the
  differentiable stub and the hard reference Box at the DRAWN trace edge; the
  trace, a node-plane sheet since #931, realizes one cell short of it, and the
  Ey edge between the two was not a conductor — a 127 µm slot, no notch
  (`SMOKE=0` on main: AD objective flat at −18 dB over L = 4–12 mm, production
  extractor |S21| = 0 dB). The root is now read back from the realized sheet
  footprint, and two build-time checks pin it: the Ey edges meeting at the root
  conduct with the stub and the one above it does not without (a slot and an
  overlap both raise), and the hardened soft occupancy equals the hard edge set.
- With the fix (`SMOKE=0`, RTX 4090): AD objective −49.4 dB at L = 7.598 mm,
  brute-scan minimum −50.8 dB at 7.5 mm, production notch −37.0 dB at
  6.027 GHz, all four script gates PASS.
