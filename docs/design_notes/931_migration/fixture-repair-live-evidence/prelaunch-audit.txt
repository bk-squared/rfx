R1: consistent with docs/design_notes/931_migration/T9-fixture-repair.md: "No MSL snapshot is refreshed without passing physical checks, a fixed-board refinement and an independently named confirmation." Current PI authority supersedes its former no-launch handoff. Consistent with short-fixture-repair.md: "the miss remains visible". No private memory index exists in this worktree.
R3: memory=T9-fixture-repair.md | R2-attempts=1 per declared final-source, fixed-mesh duration, fixed-board refinement and repaired-short hypothesis | falsifier=all six exact selector collections passed; protected rfx diff empty
Predictions before launch: MSL final-source captures pass all qualification screens, base/long within unchanged .005/.002 drift budget and base/refine raw complex <=.02 all bins. Historical golden comparison is expected red. V173A capture and lock agree at existing tolerances. Far-field both horizons retain <5% and local<scalar/2; temporal closure requires absolute power change <1% and relative L2 complex angular field change <1% separately on each mesh (new stricter convergence screen, no gate relaxation). Short has zero cross-plate transmission and observable reflection at both drives; coarse historical advisory interval remains red. Inspect analytic short phase and fixed-geometry fine-grid trends before interpretation.
Active-run list inspected: unrelated running jobs preserved. No previous fixture-repair campaign runs exist to clean. Historical 369367259538/539 are cited reference evidence in T7 and retained. No other worktree opened. Status snapshot/lab-status unavailable. Completed run reads 369367259538 and 369367259441 return Resource limits None in this CLI; no preset capacity is inferred. Use prepared explicit CPU 8/memory32Gi requests, each isolated, no GPU requested.

MSL confirm: independently named base solve required by existing capture contract; same source/mesh/horizon, predicts passing physical screens and allclose to base at unchanged .005/.002. This is independent replication, not a mechanism retry. Shell syntax passed; no pytest in this capture-only job.

Relaunch: all initial six jobs ended rc128 before checkout, exact Git administrative-directory ownership error saved in full logs. Identified orchestration defect permits retry under R2; no physical attempt occurred. Exact admin path now added to pod-global safe.directory, source SHA unchanged. All rendered shell syntax checks passed. Failed IDs retained as claims-bearing launch evidence until outcome commit.

Resource schema audit: server-normalized YAML omitted prepared top-level cpu/memory fields. Official VESSL Run YAML documents resources.requests.cpu/memory. Resource-only smoke uses that schema, predicts retained request and matching cgroup CPU/memory limits; zero FDTD work, pinned clone. This is a launcher falsifier, no physical retry.

Resource smoke369367259621 falsified requests support: server again drops request fields and cgroups are unlimited. Live resource listing provides base-pod8CPU/32Gi and cpu-32-mem-64 32CPU/64Gi. Two preset-only probes verify actual cgroup limits before replacing unfinished MSL jobs. This changes allocation mechanism from ignored custom fields to existing named presets; no physics or tolerance change.

Preset relaunch of base/long/confirm: completed resource probe369367259623 confirms cpu.max=800000/100000 and memory.max=34359738368. Existing MSL jobs612/615/616/617 terminated only after identity and full-log backup. Same physics SHA and selectors, no failed physics screen yet; allocation fix is the named implementation defect. Maintained jobs now fail before simulation on missing/unlimited/insufficient cgroup allocation.

Before any final-source MSL report exists, the offline temporal comparator additionally checks raw base/long S at the same .005/.002 budget, so projection cannot hide temporal drift. This strengthens the predeclared convergence screen; it changes no existing test threshold.

Refinement preset probe369367259624 completed rc0, cpu.max3200000/100000 and memory.max68719476736; completed run read records32CPU/64Gi. Launch refinement using same pinned physics SHA and cgroup fail-fast guard. Existing raw .02 mesh budget and physical screens unchanged.
