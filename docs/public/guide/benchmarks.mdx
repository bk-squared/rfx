---
title: "Benchmarks"
description: "Reference cases, pass criteria, required dependencies, and reproduce commands for rfx cross-validation."
sidebar:
  order: 8
---

import { Aside } from '@astrojs/starlight/components';

For validation scope and recommended engineering checks, read
[Cross-Validation and Accuracy](/rfx/guide/validation/).

`validation/crossval/manifest.json` defines the case list, dependencies, exit
codes, checks, and saved artifacts. Automated tests compare the table below
with that manifest, so a case cannot be added or reclassified without a
documentation update.

<Aside type="note" title="Pass criteria are case-specific">
  The metrics and tolerances below come from each script's docstring and check
  block. A pass applies to the stated geometry, mesh, frequency range, and
  reference only. It does not by itself validate other geometries, arbitrary
  discretizations, or an entire port or component family.
</Aside>

<Aside type="caution" title="Conductor cases are recomputed at 2.0">
  Release 2.0 changed how a conductor lands on the lattice: a PEC volume
  realizes walls at both of its drawn faces, and foil is declared as a sheet on
  one node plane
  ([How conductors land on the lattice](/rfx/guide/materials-geometry/#how-conductors-land-on-the-lattice)).
  Every case below with a declared conductor body — `05`, `06b`, `07`, `11`,
  `15`, `18`, `19`, `20` — is re-solved from the drawn geometry, with the
  compensations that used to sit in its oracle deleted rather than re-tuned;
  a case that has not been re-solved does not ship in 2.0.
  `validation/crossval/manifest.json` and each case's committed artifact are
  the live source for a gate value; a number quoted in this table and not in
  the manifest is documentation, not the gate.

  Three groups of cases are controls and must come back unchanged: the
  boundary-PEC cases (`09`, `10`, `14`, `24`), the dielectric-only cases
  (`01`–`04`, `17`, `22`, `23`), and the σ-fill cases (`16`, `21`), whose metal
  is a conductivity written into cells rather than a declared conductor and is
  fenced out of the contract.
</Aside>

## Result codes

Each script returns one of these exit codes:

| Exit | Meaning |
|------|---------|
| `0` | Every self-check and numeric criterion passed, including each reference marked `required_for_script_pass` in the manifest. |
| `1` | A required run, self-check, or numeric criterion failed. |
| `2` | The rfx self-check completed, but a required external reference or optional dependency such as OpenEMS was unavailable. This result is inconclusive, not a pass. |

Some manifest entries also define a failure-line sentinel. The CPU runner
checks that sentinel after exit `0` and treats a contradictory failure message
as a failure.

## Reference cases

**Validated comparison** means that the listed command or commands check the
references explicitly identified as gated in that row.
**Diagnostic only** means that the script reports a comparison for the stated
configuration or checks a specific regression condition; it must not be used
as evidence for broader support.

| Use | Case | Purpose | Reference | Pass criteria | Reproduce |
|-----|------|---------|-----------|---------------|-----------|
| Validated comparison | `01_waveguide_bend` | 90° dielectric waveguide-bend transmittance | **Meep** "Basics" bend tutorial and a straight-guide self-check | Smoothed mean `T ∈ [0.3, 1.0]`; straight-guide `T ∈ [0.95, 1.05]`; `\|rfx − Meep\| < 0.10` | `python validation/crossval/01_waveguide_bend.py` |
| Validated comparison | `02_ring_resonator` | Ring-resonator resonant-mode frequencies | **Meep** "Basics" ring-mode tutorial and Harminv | Every Meep mode assigned a distinct rfx mode by a tolerance-free one-to-one assignment (an unmatched Meep mode fails); at least two modes; mean **and** per-mode frequency error `< 5%`; and mode `Q` inside the exact transformed log-Q interval from the declared record-resolution scale `tau_ref/T` for each mode whose decay the harminv record observed (`T/tau >= 1/4`) — when `tau/T >= 1` the high-Q side is unbounded, and modes whose Q the record cannot resolve are reported, not gated | `python validation/crossval/02_ring_resonator.py` |
| Validated comparison | `03_straight_waveguide_flux` | Straight-waveguide guided-mode dispersion, transmission, and energy conservation | Closed-form **symmetric-slab TE0** `n_eff` for the declared recipe, the **Meep** "Basics" straight-waveguide tutorial, and the unit-transmission identity | Over the central source band (`fcen ± 0.15*df`): the measured `n_eff(f)` is within **2.0%** of the analytic slab mode at **every** bin (max, not mean), and rfx and Meep band-mean `T` are each in `[0.95, 1.05]` with a band-mean difference `< 0.05`. `T = flux_out/flux_in = 1` is an energy-conservation identity that holds for any lossless guide, so it is reported as a self-check and is not on its own evidence about the guide (issue #812). Peak-bin values are informational only. | `python validation/crossval/03_straight_waveguide_flux.py` |
| Validated comparison | `22_dispersive_slab_fresnel` | Dispersive single-pole slab (Debye, Lorentz, Drude) normal-incidence `R(f)` and `T(f)`, 4–10 GHz, on the `04_multilayer_fresnel` rig with the record length derived from the slab's own ring-down (−40 dB settling witness) | Analytic transfer matrix with the complex, frequency-dependent ε(f) of the declared pole, gated per bin and band-mean; **Meep** at 40 px/cm (the converged rung of a committed resolution ladder) with the same material (mapped into `LorentzianSusceptibility` / `DrudeSusceptibility` by `validation/crossval/comparators/dispersive_eps.py`, unit-tested to 1e-9 with wrong-convention falsifiers) is gated against the transfer matrix and rfx is gated against Meep. | Band-mean \|ΔR\| / \|ΔT\| vs the transfer matrix: Debye `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_dR_gated = 0.0023` / `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_dT_gated = 0.0028`, Lorentz `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.mean_dR_gated = 0.0028` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.mean_dT_gated = 0.0016`, Drude `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.mean_dR_gated = 0.00049` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.mean_dT_gated = 0.0017` against derived means of `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_window_R = 0.01` / `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.mean_window_T = 0.017` (`04_multilayer_fresnel` committed envelope × 1.5 plus the ADE's own temporal-discretization term); rfx vs Meep: Lorentz `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dR_rfx_meep_gated = 0.004` / `validation/crossval/_22_dispersive_results/rfx.json::arms.lorentz.meep.mean_dT_rfx_meep_gated = 0.004`, Drude `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dR_rfx_meep_gated = 0.0017` / `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dT_rfx_meep_gated = 0.0078`; verdict `validation/crossval/_22_dispersive_results/rfx.json::verdict.exit_code = 0`. Pre-declared before any run in `docs/design_notes/20260902_cv22_dispersive_slab_predeclaration.md` with falsifiers (pole frequency or relaxation time wrong, dispersion removed, Meep material mapped with the wrong convention) that each exited 1, e.g. `f0 × 1.3`: `validation/crossval/_22_dispersive_results/rfx__falsifier_lorentz_f0_x1p3.json::arms.lorentz.mean_dR_gated = 0.093` in R. Scope: normal incidence only, one pole per model (Debye, Lorentz, Drude), no conductivity-plus-pole mix, 4–10 GHz, one slab thickness (10 mm) and one mesh (dx = 1 mm); the documented user path `Simulation.add_material(..., debye_poles=/lorentz_poles=)` (materials-geometry guide) is NOT exercised — the case drives `update_e_debye` / `update_e_lorentz` directly, as `04_multilayer_fresnel` drives `update_e`. The Meep reference (40 px/cm) is converged only one rung under the window: Drude Meep-vs-transfer-matrix `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_dT_meep_tmm_gated = 0.0095` against `validation/crossval/_22_dispersive_results/rfx.json::arms.drude.meep.mean_window4_T = 0.017` (1.8×); the 20 px/cm rung would fail it. **Continuum gate + exact-lattice witness at every rung** (docs/design_notes/20260903_lattice_witness_standard.md): the continuum windows above are unchanged; each of the three committed dx rungs additionally passes \|rfx − exact 1-D Yee lattice\| ≤ `W_witness(f)`, per bin and band-mean, with `W_witness` derived from the lattice model's own error budget (record truncation, incident-reference truncation, float32) and never fitted (the float32 term is a statistical estimate, not a bound: it is the √-accumulation size `N ε32/√2`; the coherent worst case is computed and reported, never gated) — `validation/crossval/_22_dispersive_results/lattice_witness.json::verdict.all_rungs_ok`, `validation/crossval/_22_dispersive_results/lattice_witness.json::verdict.n_rungs = 3`, band-mean \|rfx − lattice\| in R `validation/crossval/_22_dispersive_results/lattice_witness.json::rungs.lorentz.mean_dR_lattice_gated = 0.00025` against `validation/crossval/_22_dispersive_results/lattice_witness.json::rungs.lorentz.mean_W_witness_R_gated = 0.0026`. The lattice model predicts this case's whole transfer-matrix residual a priori (0.0022 / 0.0028 / 0.00049 against the measured 0.0023 / 0.0028 / 0.00049). On the Debye arm the wrong-model falsifier does not fire — that arm's record settles only to `validation/crossval/_22_dispersive_results/rfx.json::arms.debye.tail.scat_refl_rel = 0.0072`, so its truncation term dominates its lattice term and its lattice gate is declared non-discriminating. That limitation is a property of the RUNG, not the arm: §8.1's pre-declared remedy rung (`--settling-bar 3e-4`, committed as `validation/crossval/_22_dispersive_diag/lattice_witness.json`) ran on the 2026-09-03 VESSL leg and came out on the first branch of its own prediction — `W_witness,R` 0.017 → `validation/crossval/_22_dispersive_diag/lattice_witness.json::rungs.debye_tail3e4.mean_W_witness_R_gated = 0.000332`, \|rfx − lattice\| 6.5e-4 → `validation/crossval/_22_dispersive_diag/lattice_witness.json::rungs.debye_tail3e4.mean_dR_lattice_gated = 1.06e-05`, the wrong-model falsifier now firing at 6.68× on all 229 gated bins — while the arm's residual against the continuum transfer matrix stayed put at `validation/crossval/_22_dispersive_diag/rfx__debye_tail3e4.json::arms.debye.mean_dR_gated = 0.0022`. That rung is a committed DIAGNOSTIC in its own directory and changes none of cv22's three committed rungs. **Recorded consequence** (note §5.4): the slab family's continuum gate is one slab's discretisation error measured inside a window derived from another slab's discretisation error — re-deriving `W_mean,R` from the non-lattice part of the same measurement (`gate_from_envelope(1.69e-4, quantum=1000)` = 0.001) fails 7 of the 12 committed rungs, and every measured residual equals its own lattice term to two significant figures. No window is changed. | `python validation/crossval/22_dispersive_slab_fresnel.py`<br />`python validation/crossval/22_dispersive_slab_fresnel.py --lattice-witness`<br />`pytest -q tests/crossval/test_cv22_dispersive_slab_gates.py tests/crossval/test_cv22_dispersive_eps_mapping.py tests/crossval/test_lattice_witness_gates.py` |
| Validated comparison | `23_lossy_slab_fresnel` | Lossy (conductive) slab, ε' = 4, 10 mm, σ from tan δ 0.1 / 1 / 3 at 7 GHz: normal-incidence `R(f)`, `T(f)` and the absorption `A(f) = 1 − R − T`, 4–10 GHz, on the `22_dispersive_slab_fresnel` rig (tand0p1 / tand1 at dx = 1 mm, tand3 at dx/2). Scope: normal incidence only, one thickness (10 mm), σ-only loss (no pole, no σ + pole mix), 4–10 GHz. | Analytic transfer matrix with the complex ε' − jσ/(ωε0), gated per bin and band-mean on R, T **and A**; **Meep** with `D_conductivity = σ·a/(c ε0 ε')` (mapping unit-tested to 1e-9 with wrong-scaling falsifiers) at `validation/crossval/_23_lossy_results/meep_tand0p1.json::resolution = 80` px/cm (tand0p1) and `validation/crossval/_23_lossy_results/meep_tand3.json::resolution = 40` px/cm (tand1, tand3) gated against the transfer matrix and rfx gated against Meep. Two arms use the documented `Simulation.add_material(..., sigma=)` + `add(Box)` path (arrays asserted equal to the direct construction); the time stepping is the same `update_e` in every arm. | Band-mean \|ΔR\| / \|ΔT\| / \|ΔA\| vs the transfer matrix: tand0p1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dR_gated = 0.0039` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dT_gated = 0.0057` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_dA_gated = 0.002` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.mean_window_A = 0.027`, tand1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dR_gated = 0.0051` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dT_gated = 0.0018` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_dA_gated = 0.0033` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand1.mean_window_A = 0.027`, tand3 `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dR_gated = 0.0031` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dT_gated = 3e-5` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_dA_gated = 0.0031` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_R = 0.01` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_T = 0.017` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.mean_window_A = 0.027` — the T gate on tand3 is vacuous as pre-declared (T_TMM ≤ 0.003 < 0.017 everywhere; R and A carry that arm) (windows: the `04_multilayer_fresnel` committed envelope × 1.5 plus the σ update's own temporal-discretization term; A windows are the triangle sums); rfx vs Meep: tand0p1 `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_dR_rfx_meep_gated = 0.0052` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_dA_rfx_meep_gated = 0.0028`, tand3 `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.meep.mean_dR_rfx_meep_gated = 0.0022` / `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.meep.mean_dA_rfx_meep_gated = 0.0024`; verdict `validation/crossval/_23_lossy_results/rfx.json::verdict.exit_code = 0`. The exact Yee-lattice solution of the staircase slab reproduces every arm (\|rfx − lattice\| `validation/crossval/_23_lossy_results/rfx.json::arms.tand3.lattice.mean_dR_lattice_gated = 3e-5` on tand3) and is why tand3 runs at dx/2 — resolution, not tolerance; no physics window or gate moved in three rounds (one diagnostic assertion, the tail-fit block count, was relaxed after the measurement and the tail-fit keys post-processed — stated in the note's §15). Pre-declared in `docs/design_notes/20260902_cv23_lossy_slab_predeclaration.md` with falsifiers that each exited 1 (σ × 1.5, σ = 0, a gain arm with R + T > 1, Meep σ_D mis-scaled), e.g. σ = 0 on tand3: `validation/crossval/_23_lossy_results/rfx__falsifier_tand3_sigma_zero.json::arms.tand3.mean_dA_gated = 0.59` in A. The tand0p1 Meep reference is converged one rung under the window: Meep-vs-transfer-matrix `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_dR_meep_tmm_gated = 0.0078` against `validation/crossval/_23_lossy_results/rfx.json::arms.tand0p1.meep.mean_window4_R = 0.01` (1.3×, with a known +a/80 thickness systematic in the reference: Meep minus TMM(d + a/80) is 8.8e-5 in the mean); the 40 px/cm rung would fail it — Meep's first-order term is a one-cell thickness excess of a `Block` without `eps_averaging` (committed ladders and a centre-shift leg at `validation/crossval/_23_lossy_results/meep_ladder_summary.json::diagnostics.tand0p1.res40_shift_half.mean_dR_meep_tmm_gated = 0.00024`). **Continuum gate + exact-lattice witness at every rung** (docs/design_notes/20260903_lattice_witness_standard.md): the continuum windows above are unchanged; all nine committed dx rungs (three arms × dx, dx/2, dx/4) additionally pass \|rfx − exact 1-D Yee lattice\| ≤ `W_witness(f)` per bin and band-mean, `W_witness` derived from the lattice model's own error budget (record truncation, incident-reference truncation, float32 — the float32 term a statistical estimate, not a bound) and never fitted (`validation/crossval/_23_lossy_results/lattice_witness.json::verdict.all_rungs_ok`, `validation/crossval/_23_lossy_results/lattice_witness.json::verdict.n_rungs = 9`; worst per-bin ratio over every rung and observable `validation/crossval/_23_lossy_results/lattice_witness.json::rungs.tand3_dx4.worst_ratio_T = 0.3`). This turns round 3's reported lattice witness into a gate at every rung, and it un-vacuums the `tand3` transmission observable the continuum gate cannot resolve. **Recorded consequence** (note §5.4): the slab family's continuum gate is one slab's discretisation error measured inside a window derived from another slab's discretisation error — re-deriving `W_mean,R` from the non-lattice part of the same measurement (`gate_from_envelope(1.69e-4, quantum=1000)` = 0.001) fails 7 of the 12 committed rungs, and every measured residual equals its own lattice term to two significant figures. No window is changed. | `python validation/crossval/23_lossy_slab_fresnel.py`<br />`python validation/crossval/23_lossy_slab_fresnel.py --lattice-witness`<br />`pytest -q tests/crossval/test_cv23_lossy_slab_gates.py tests/crossval/test_cv23_lossy_eps_mapping.py tests/crossval/test_lattice_witness_gates.py` |
| Validated comparison | `04_multilayer_fresnel` | Normal-incidence slab `R(f)` and `T(f)` | Analytic transfer-matrix/Fresnel solution (Taflove Ch. 5) and, when it is importable, **Meep**. Both references decide the verdict since #812: before it, Meep's *availability* was required for exit `0` but every Meep number was printed and none was gated. | Against the analytic result, mean `T` error, mean `R` error, and `R+T` energy deviation are each `< 0.05`; every masked bin additionally satisfies `\|R+T−1\| ≤ 0.06`; a post-pulse settling-tail witness is gated; and a **fringe-resolved** gate compares each Fabry–Perot extremum of `R(f)` one at a time — position within `W(f) = 2·(df/2 + Yee dispersion shift)` = 59/106/235 MHz at the 3.75/7.50/11.24 GHz fringes, value within `0.04` (11% of the 0.36 fringe contrast). When Meep runs, `max\|Meep − analytic\| ≤ 0.08`, `max\|rfx − Meep\| ≤ 0.16`, and Meep's own fringe positions are gated on its native flux grid. A band mean over an interference pattern is blind to shape error: with the mean gates alone the case passed while the true `eps` was 12.33% away (measured: `T` mean err 0.0494, `R` 0.0449, both under 0.05). **Continuum gate + exact-lattice witness at every rung** (docs/design_notes/20260903_lattice_witness_standard.md): `--lattice-witness` writes the exact 1-D Yee-lattice prediction of this very slab and the derived `W_witness` beside the run (`validation/crossval/_04_fresnel_results/lattice_witness.json`). This case now runs a settling-extension loop (grow the interior box until both post-pulse tails clear the family's −40 dB bar, the same mechanism cv22/cv23 already had) instead of a fixed 719-step record — the committed rung reaches `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.n_steps = 990`, and the lattice witness is now GATED, not merely reported: `W_witness` is `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_W_witness_R_gated = 0.000635` in R and `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_W_witness_T_gated = 0.000291` in T, in the gated mean, both comfortably inside this case's own band-mean windows. **This rung's committed band-mean \|ΔR\| of 0.0066 is this slab's Yee-lattice second-order term, not solver error** — confirmed pre-fix on cv23's sibling `sigma_zero` arm (`validation/crossval/_23_lossy_results/rfx__falsifier_tand0p1_sigma_zero.json`, \|rfx − lattice\| 1.7e-4 against `W_witness` 5.5e-3) and now directly at this rung's own settled record too (\|rfx − lattice\| `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_dR_lattice_gated = 2.01e-05`). **Scoped to \|ΔR\|:** the lattice term is the same number in R and T (0.00727 over this case's 170-bin mask; the slab is lossless, so A ≡ 0 and \|Δlattice−TMM\| is equal in the two), but the committed envelopes are 0.0066 in R (lattice/envelope 1.10) and 0.011 in T (0.66), and at cv04's own settled rung \|rfx − lattice\| in T is `validation/crossval/_04_fresnel_results/lattice_witness.json::rungs.slab_eps4.mean_dT_lattice_gated = 9.52e-05` — far below even the pre-fix 0.011 T envelope, consistent with that envelope having been truncation-dominated, though a re-derived envelope (issue #928) is the next step before restating what fraction it now covers. | `python validation/crossval/04_multilayer_fresnel.py`<br />`python validation/crossval/04_multilayer_fresnel.py --lattice-witness`<br />`pytest -q tests/crossval/test_lattice_witness_gates.py` |
| Diagnostic only | `05_patch_antenna` | 2.4 GHz rectangular patch resonance on FR4 (coarse-mesh integration check) | **OpenEMS** "Simple Patch Antenna" tutorial and **Balanis** transmission-line estimate, both reported not gated | Reports a coarse-mesh (dx=1 mm) resonance and exercises the probe-fed patch / finite-GP / CPML / non-uniform-z / openEMS-lumped-port path end to end. It does not gate accuracy; since issue #812 the reported resonance is the mode identified as TM100 of the declared spectrum (a mis-realized 22.0 mm patch is assigned TM110 and fails by name: `tests/fixtures/patch_mode_identification/cv05_ringdown_spectra.json::runs.patch_len_22p0mm.modes[1].freq = 3.043213 GHz`), an identification window of [−11.11 %, +12.50 %] that is not an accuracy claim — and, fired against the instrument, a 38.0 mm build passes on the committed fixture because a weak extra pole (`tests/fixtures/patch_mode_identification/cv05_ringdown_spectra.json::runs.patch_len_38p0mm.modes[1].freq = 2.702632 GHz`) inside the window is named TM100; the committed patch evidence lives in `tests/locks/test_patch_edgefed_resonance_harminv.py`, `tests/locks/test_patch_edgefed_s11_passivity.py`, `tests/oracle/test_patch_cavity_eps_oracle.py`, and `tests/crossval/test_patch_canonical_farfield_e4.py` (committed thirds-rule OpenEMS far-field reference, envelope-locked). These are signed-envelope regression locks and characterizations, not accuracy claims — the harminv gate pins a measured ~−6% offset from the realized-raster Balanis value WITH its sign, and says so in its own docstring; the pre-#702 9.32/9.21 GHz patch numbers are retired (issue #782). | `python validation/crossval/05_patch_antenna.py`<br />`pytest -q -m slow tests/crossval/test_patch_canonical_farfield_e4.py` |
| Validated comparison | `06b_msl_notch_filter_uniform` | MSL open-stub notch filter using distributed `add_msl_port` on a uniform mesh | Analytic quarter-wave notch plus a separately gated, committed **OpenEMS** dx=50 µm result | Script gate (re-derived 2026-09-01, issue #812 P3): notch frequency located by sub-bin log-parabolic vertex, error `< 4.0%`; `−10 dB` stopband fractional width within `±20%` of the ideal shunt-open-stub closed form `0.210274`; half-grid resolution witness `< 1.000` bin; median `Z₀ ∈ (40, 65) Ω`. The `< −10 dB` depth check is RETAINED as a witness only — the worst sampled minimum on this grid is `−31.23 dB` (`−31.51 dB` in the #931 re-solve's own sweep), so it cannot fail while a notch exists. **Current run — the post-#931 re-solve (VESSL 369367259191, 2026-09-07; the trace and the stub are declared as SHEETS, log `validation/crossval/_06b_notch_uniform_logs/20260907T124851Z_run.log`)**: frequency error 2.16 %, stopband width ratio 0.9991, half-grid witness 0.4469 bin, median Re(Z₀) 48.2 Ω, notch depth −39.4 dB — every gate passes, and the same job's build falsifiers put the one-cell stub shift at 0.823 % of a predicted 0.532 %, so the falsifier that fired below no longer fires. The same drawing realized as one-cell PEC Boxes read 1.40 % / −43.3 dB / 46.5 Ω (2026-08-27, pre-2.0). Which trace width the analytic reference takes is NOT settled by the re-solve: the pre-declared Z₀ falsifier (46.48 ± 1.0 Ω for the electrical 635.0 µm, ≈49.4 Ω for the geometric 571.5 µm) landed between them at 48.2 Ω — crossval-B's adjudication: both readings rejected, nothing re-derived from one board at one dx, and the regenerated falsifier summary committed (fae08d10), so the citations that follow resolve against it. Judged on this board on 2026-09-02 (issue #812 round 2, VESSL 369367257702 — PRE-2.0 realization): every gate passed — notch error 1.453 %, width ratio 0.9684, witness 0.3175 bin — and a 5-cell stub fired the width gate while the depth witness stayed blind; one pre-declared falsifier FIRED there: a one-cell stub-length error moved the refined notch estimate 0.145 % against a predicted 0.532 %. On the sheet board (VESSL 369367259191) the committed summary reads notch error `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::criterion_A_baseline.err_pct = 2.1649` %, width ratio `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::criterion_A_baseline.bw_ratio = 0.9991`, witness `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::criterion_A_baseline.witness_bins = 0.4469` bin, and the same arm reads `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::stub_1cell.refined_delta_pct = 0.823` % against `validation/crossval/_06b_msl_notch_results/cv06b_build_falsifiers_summary.json::stub_1cell.true_shift_pct = 0.532` % — over-response (1.55x) where there was under-response (0.27x), so that falsifier no longer fires (neither response attributed; visibility, not accuracy; the 4 % verdict does not depend on it). The committed 2026-08-27 run predates the two new gates (`< 15%` / `< −10 dB` were the gates in force then, history not current). Fixture-test gate: both results passive, notch-frequency difference `≤ 7%`, off-notch mean/max `\|S21\|` difference `≤ 0.13/0.25`. | `python validation/crossval/06b_msl_notch_filter_uniform.py`<br />`pytest -q tests/crossval/test_msl_notch_e4_comparison_gates.py` |
| Diagnostic only | `07_sheen_lpf` | Sheen 1990 stepped-impedance microstrip low-pass filter stopband | **OpenEMS** matched-geometry run, whose own passivity and `Re(Z₀)` sanity are gated first, plus a committed **Palace** FEM conformal-mesh referee (`tests/fixtures/sheen_lpf_e4/`) | Characterization locks only, no accuracy gate. The referee shows the stopband is a double transmission zero (~7.0 and ~8.0 GHz) that OpenEMS matches to ~0.7%. The rfx leg was regenerated 2026-07-26 with the measured recipe (num_periods = 60, settling witness −72 dB vs the −24.7 dB truncation failure of the original num_periods = 20 leg; n_probe_offset = 30, which removes a measured probe near-field contamination — moving the rfx argmin from 7.218 to 7.874 GHz, the ~8 GHz doublet member, 1.4% from OpenEMS — while keeping the λ_g/4 reflector clearance that offset = 40 violated). The current #931 sheet leg and derived referee were regenerated by VESSL 369367259192 (2026-09-07, commit 069bdebc): raw-bin rfx argmin 8.202 GHz vs OpenEMS 7.983 GHz (2.74% split); parabolic FDTD argmins 8.244069 / 7.994749 GHz; structure distance 2.87% vs OpenEMS 0.66%, distinct from the separately windowed rfx argmin distance to Palace of 2.3756%. Quoted rfx `\|S\|` is passivity-enforced (strict `≤ 1`, max column power 0.9995); the raw excess is recorded per bin in `passivity_correction` (3/120 bins > 0.05 on the current leg, worst 0.6572 at 17.870 GHz; all three are above 17 GHz, none inside the 5–15 GHz null band. The case characterizes this as a high-frequency residual on a coarser realized strip at the same dx = 200 µm, not evidence of a physics regression in the stated null band; no magnitude-accuracy claim is made — the pre-#516 leg's 84/120 bins, worst 0.365 at 18.0 GHz, was substantially extractor defects, not a coarse-mesh envelope). The fitted passband median `Re(Z₀)` (51.9 Ω vs OpenEMS's ~51 Ω) is printed, not gated — the old ~67 Ω fit-conditioning artifact is gone; the in-band 5–15 GHz median is 54.7 Ω. Gated: OpenEMS reference passive (column power `≤ 1.10`), median `Re(Z₀) ∈ (40, 65) Ω`, passband mean `\|S21\| ≥ 0.85`, stopband zero `≤ −20 dB`; committed bin counts; the committed argmin nulls to `1%`, the rfx passband mean to `1%`, and the referee verdict; and the evidence chain (strict bound, settling witness < −40 dB, correction footprint) failing closed when a leg lacks the witness/enforcement fields. Re-gated 2026-09-01 (issue #812 mechanism P3, `docs/design_notes/estimator_resolution_regate.md`): the argmin null was bin-quantised at 163.866 MHz = 2.081% per bin against a declared 1% window, so erasing one of the two transmission zeros passed all 17 gates. The argmin lock is kept and four families were added — both doublet members per solver located by sub-bin log-parabolic vertex and locked to `±0.50%` against the committed referee fixture's own values; the transmission-zero COUNT (`== 2`, depth `≤ −20 dB`); the `−3 dB` corner frequency (`±0.25%`), which this case never gated; and an in-run half-grid resolution witness that a bin-quantised estimator scores exactly `1.0000` bin on and cannot pass. The #931 regeneration records 28/28 passing gates on the committed legs; the estimator-falsifier fixture remains a pre-#931 calibration, so it does not certify the regenerated leg's defect response. | `python validation/crossval/07_sheen_lpf.py`<br />`pytest -q tests/crossval/test_sheen_lpf_palace_referee_gates.py` |
| Validated comparison | `09_half_symmetric_waveguide` | PMC symmetry plane versus a full PEC cavity for TE₁₀₁ | Analytic rectangular-cavity result (Pozar Ch. 6) and full-versus-half self-check | Realized geometry (issue #812): every solved extent, and the realized PMC mirror plane `a_eff = 2 ×` (H_tan wall), within `dx/4 = 127 µm` of declared; `f_full` and `f_half` are each within `10%` of analytic; `\|f_full − f_half\|/f_full < (d²/(a²+d²))·(dx/4)/a = 0.356%` (tightened from `5%`, which tolerated a full cell of mirror-plane error) | `python validation/crossval/09_half_symmetric_waveguide.py` |
| Diagnostic only | `10_pmc_cpml_half_symmetric` | PMC+CPML per-face composition on uniform and nonuniform grids | Peak-stability self-check, bit-exact PMC realization, and a mirrored full-domain image-source amplitude control; no external solver | For each path: peak spread `(max−min)/max < 0.02`; no NaN or Inf; `max\|H_tan\|` on the declared `y_lo` face exactly `0.0` at every `cpml_layers` (with a non-degeneracy guard that the field is non-zero somewhere); and the half-domain PMC peak within `2%` of the equivalent full-domain in-phase image-pair peak at `cpml_layers = 8`. The last two exist because the first is a RELATIVE spread that a constant amplitude factor cancels — issue #812 measured that deleting `apply_pmc_faces` improves it from 0.2487% to 0.0667%. | `python validation/crossval/10_pmc_cpml_half_symmetric.py`<br />`pytest -q tests/crossval/test_crossval10_pmc_regate.py` |
| Diagnostic only | `11_waveguide_port_wr90` | WR-90 port results for empty guide, PEC short, and a dielectric slab | Analytic matched-guide, short-circuit, and Airy references are gated; optional Meep, OpenEMS, and Palace result files populate comparison tables | Empty guide: band-mean linear-magnitude difference `< 0.02` for S11 and `< 0.03` for S21. PEC short: band-mean magnitude difference `< 0.05`, band-mean round-trip phase difference `< 15°`, per-frequency `\|S11\| ∈ [0.93, 1.07]`, and a `max\|S11\| ≤ 1.05` passivity ceiling (a start-up self-test proves the per-frequency gate rejects a synthetic spike). Slab: band-mean magnitude difference `< 0.10` for S11 and `< 0.07` for S21; masked band-mean phase difference `< 60°` where `\|S_ref\| ≥ 0.30`; maximum complex-S difference `< 0.30`. | `python validation/crossval/11_waveguide_port_wr90.py` |
| Validated comparison | `14_rect_cavity_pozar` | Rectangular air-filled PEC cavity eigenfrequencies | Exact closed-form **Pozar** cavity resonance `f_mnl = (c/2)·√((m/a)²+(n/b)²+(l/d)²)`, re-derived in the script; no external solver | Wall registration: the effective separation `(n−1)·dx` must equal `(a,b,d)` to within `1e-9 m` on every axis; TE₁₀₁ frequency error `< 1%`; **every** one of TE₁₀₁/TM₁₁₀/TE₀₁₁/TM₁₁₁/TE₂₀₁/TM₂₁₀/TE₁₀₂ within `2%` (max over all seven, and a mode that cannot be extracted fails the gate); and each mode within one tenth of the record's Fourier bin of the closed-form **discrete-Yee** eigenvalue of the same box, a second exact oracle also re-derived in the script. For the `(a,b,d) = (50,30,40) mm` cavity on a dx = 1 mm mesh that divides all three extents exactly. The cavity is closed and lossless, so the ring-down settling witness does not apply and harminv `Q` is a window-length artifact that is printed but not gated; only the frequency is gated. | `python validation/crossval/14_rect_cavity_pozar.py` |
| Validated comparison | `24_nu_rect_cavity_pozar` | The `14_rect_cavity_pozar` cavity on GRADED-z meshes: uniform 1 mm control, one 0.5 mm band, small–large–small–large, and uniform 0.5 mm as the cost control. Scope: one closed lossless PEC cavity, z grading only, chain ratio ≤ 1.4 (the #785 envelope), seven modes below 8.5 GHz; no S-parameters, ports, absorbers, materials or in-plane grading. | Exact closed-form **Pozar** spectrum (E2) with the exact discrete Yee-lattice eigenfrequency of each arm's own grid as the a-priori witness, gated to `4 ppm` (`validation/crossval/_24_nu_cavity_results/rfx.json::declared.w_est = 4e-06`); the #810 allowance gate on the equal-dt spatial eigenvalue with the allowance derived from #785's per-transition reflection budget; mode count 7 by `(m, n, l)`; #785's energy envelope; two-window stationarity. | Pre-declared before the run (`docs/design_notes/20260902_cv24_nu_cavity_predeclaration.md`) and measured (VESSL 369367257837, every gate true on every arm, `validation/crossval/_24_nu_cavity_results/rfx.json`): lattice residual ≤ 0.48 ppm on every arm and mode, ≤ 0.08 ppm on the graded arms (`validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.resid_lattice = -0.00001 %`); TE101 `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE101.dev_raw = -0.00833 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE101.dev_raw = -0.01497 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE101.dev_raw = -0.01411 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform_fine.rows.TE101.dev_raw = -0.00205 %` and TE102 `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE102.dev_raw = -0.05202 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE102.dev_raw = -0.08214 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.multi_band.rows.TE102.dev_raw = -0.01821 %` / `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform_fine.rows.TE102.dev_raw = -0.01294 %` vs Pozar on the four arms, each within 0.5 ppm of its prediction; five falsifiers each exit 1 as declared. Read plainly: the graded-z mesh is within the derived allowance of the uniform mesh it refines (equal dt: within 11 ppm on the l = 1 modes, +107 ppm worse on TE102 for the single band, `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.single_band.rows.TE102.dev_spatial = -0.10157 %` vs `validation/crossval/_24_nu_cavity_results/rfx.json::evaluations.uniform.rows.TE102.dev_spatial = -0.09091 %`, and 532 ppm better for the two-band profile; raw, at each arm's own CFL dt, 60–300 ppm worse on every l ≤ 1 mode) and the grading adds no error beyond the operator's own metric (lattice residual ≤ 0.08 ppm) — but it is NOT as accurate as uniform-fine: it buys only the z-share of each mode's error (78 % of uniform-fine's TE102 improvement at 15 % of the cells, `validation/crossval/_24_nu_cavity_results/rfx.json::arms.multi_band.cost.n_cells = 73500` vs `validation/crossval/_24_nu_cavity_results/rfx.json::arms.uniform_fine.cost.n_cells = 480000`; nothing on the l = 0 modes); the #810 reflection-derived allowance is a loose bound for eigenfrequencies (≤ 17 % used; its bound contains the control's own dispersion and cannot fail for an in-envelope profile), the lattice gate (4 ppm) carries the whole mesh claim. | `python validation/crossval/24_nu_rect_cavity_pozar.py` (VESSL: `scripts/vessl_cv24_nu_cavity.yaml`) |
| Diagnostic only | `15_patch_antenna_rt5880` | Probe-fed 40×50 mm patch on RT/Duroid 5880 (`eps_r` 2.2, h = 3.175 mm), uniform-mesh integration study | **OpenEMS** same-geometry run is gated; its canonical "Simple Patch Antenna" tutorial reproduce-check and a **Balanis** transmission-line estimate are reported, not gated | Same-geometry integration envelope, not a patch-accuracy claim: rfx ring-down `f0` versus the OpenEMS `S11` dip within `8%` (issue #812: blind to the #740 one-plane-ground realization, `validation/crossval/_15_patch_results/rfx_one_plane_ground_b29f9de7.json::f_harminv_hz = 2471858108` Hz vs `validation/crossval/_15_patch_results/openems.json::f_dip_hz = 2330000000` Hz, +6.09 % — only the build assertion catches it; a ratio-band re-gate was tried and withdrawn as an honest STOP; **that realization is pre-2.0** — from 2.0 a conductor is a volume, a sheet or a wire by declaration and this ground is a sheet on the substrate floor, so the cited figure is the dated record of what the old realization cost and is not reproducible on the current tree. The 8 % gate's blindness is a property of the gate and is unchanged by that), the −40 dB open-domain settling witness, passivity on both legs (`max\|S11\| ≤ 1.05`), and broadside directivity within `3 dB`. The substrate is asserted to rasterize to exactly four z-cells on the uniform mesh before any `f0` is quoted. Patch evidence remains delegated to `05_patch_antenna` and the committed tests it names (regression locks and characterizations, not accuracy claims). | `python validation/crossval/15_patch_antenna_rt5880.py` |
| Diagnostic only | `16_pec_sphere_mie_ka_sweep` | PEC-sphere monostatic RCS versus the exact Mie series, ka 0.5–4.0 | Exact conducting-sphere **Mie** backscatter series (Ruck 1970), re-implemented twice (the script's committed self-witnessing oracle, and again in the frozen-fixture gates from `scipy.special`; a four-way convention-independent check is recorded in the PR #475 review); no external solver | Two-tier posture at a derived CPU-scale operating point (cells-per-radius 6.4 coarse / 12.8 fine — the sphere is never cell-starved at low ka). Gated: coarse ka ≤ 1.25 and the fine rung at ka = 2.0 only, at gate = round-up(measured clearance-scan envelope × 1.5); the envelope population (a 7-point clearance scan plus three domain realizations) is committed and the gate test recomputes it, so gate and envelope cannot drift apart. Reported, **not** gated: coarse ka ≥ 1.5 and fine ka = 3.0/4.0 — near the deep Mie interference nulls, under a domain-size-only change, the domain-to-domain spread reaches 8.0 dB (coarse, at ka = 1.75) and 14.5 dB (fine, at ka = 3.0 across the committed clearance scan), the worst single-point deviation is 11.1 dB (coarse ka = 3.0) / 9.3 dB (fine ka = 3.0), the local-minimum positions move with domain size, and fine ka = 4.0 fails a 3.5 dB gate at 9 of 13 clearances (anti-aliasing scan; a 3-clearance sample can land entirely on passing values). Attribution: truncation, domain and clearance witnesses are committed fixture data; the effective-radius and 9.6-rung resolution probes are recorded as provenance of the 2026-07-27 offline session, not data; incident leakage at the monostatic bin is excluded by the documented backscatter leakage null; a sub-vs-unsub probe adds no second witness, since `monostatic_rcs` is unsubtracted by construction. Extends, does not replace, the committed coarse ladder and fine ka≈1 fixtures. | `python validation/crossval/16_pec_sphere_mie_ka_sweep.py`<br />`pytest -q tests/crossval/test_rcs_mie_ka_sweep_gates.py` |
| Diagnostic only | `17_dielectric_sphere_mie` | Lossless dielectric sphere (`eps_r` 2.56) monostatic RCS versus the exact Mie series, ka 0.5–2.5 | Exact **Bohren–Huffman** dielectric Mie backscatter series, re-implemented twice with self-witnesses (Rayleigh, m→1, convergence, unitarity); no external solver | The material-path twin of the PEC ka-sweep and the first cross-method record of the **binary** dielectric rasterize path (no sub-cell interface averaging exists — this measures that staircase, it does not certify smooth interfaces). Single gated tier: coarse ka ≤ 1.25 within 6.3 dB = round-up(committed 7-point clearance-scan envelope 4.18 dB × 1.5), a regression lock rather than a converged-accuracy claim. No fine rung is gated — a measured decision: the committed cpr-12.8 witness scan envelopes 3.75/3.04 dB, barely below the coarse 4.18 (single-clearance apparent convergence is the aliasing class the PEC review exposed). Reported, **not** gated: every bin with ka ≥ 1.5 — domain-size-only spreads of 11.7 dB (ka = 1.75) and 29.0 dB (ka = 2.5), worse than the PEC twin; truncation falsified by direct probes; incident leakage at the monostatic bin excluded by the documented backscatter leakage null (`monostatic_rcs` is unsubtracted by construction, so a sub-vs-unsub probe is a same-array tautology for this observable). Bempp PMCHWT corroboration is an offline follow-up. **Issue #812 re-gate:** the dB tier is an RCS-agreement envelope, **not** a permittivity calibration — the 6.3 dB gate accepts a rasterized `eps_r` across the committed model pass set `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[0][0] = 2.0`–`validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[0][1] = 4.5` and `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[1][0] = 5.0`–`validation/crossval/_17_dielectric_results/material_blind_window.json::summary.pass_runs_eps[1][1] = 5.6`, with a fail island `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.fail_islands_eps[0].eps_r[0] = 4.6`–`validation/crossval/_17_dielectric_results/material_blind_window.json::summary.fail_islands_eps[0].eps_r[3] = 4.9` (worst bin `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.fail_islands_eps[0].max_abs_delta_db = 9.787` dB) on the ka = 1.25 Mie resonance, first failing below at `validation/crossval/_17_dielectric_results/material_blind_window.json::summary.first_failing_eps_below = 1.8`; live defect runs at 2.0 / 5.5 (pass) and 1.8 / 6.0 (fail) agree with the model, while inside the island the live solver passes at 4.6–4.8 and fails only at 4.9 (`validation/crossval/_17_dielectric_results/cv17_permittivity_island.json::summary.n_verdicts_agree_with_model = 3` of 6 — the model over-predicts on the resonance, the live window is wider), because the Mie oracle moves only about 10 dB per unit *relative* permittivity at the gated ka (9.816/10.202/9.978/7.134 at ka = 0.50/0.75/1.00/1.25) and the gate is already round-up(envelope × 1.5). The permittivity is gated on a channel of its own instead: the value read back out of the rasterized array must sit within 0.5% relative of the declared 2.56 (half the gate's 0.1 dB reporting quantum divided by that worst slope), and the array must hold exactly two distinct values — the first check anywhere of the **binary**-rasterize claim this row's scope rests on. | `python validation/crossval/17_dielectric_sphere_mie.py`<br />`pytest -q tests/crossval/test_rcs_dielectric_sphere_mie_gates.py` |
| Diagnostic only | `18_wr90_iris_modematch` | Single symmetric inductive iris in WR-90, flux-normalized \|S11\| versus a mode-matching oracle, 8.2–12.4 GHz (29 points) | **TEn0 mode-matching cascade** (two H-plane width-step junctions + a below-cutoff section), re-implemented twice with self-witnesses (unitarity 1.1e-16; Marcuvitz cot² thin-limit anchor 10.8%); the pre-merge review independently reproduced it with a formulation-independent 2-D H-plane FDFD to 6e-4; no external solver | The first calibrated evidence for a PEC obstacle in the rectangular-waveguide S-parameter lane (iris/post/septum remain experimental; multi-iris filters stay fenced — this is the stage-1 prerequisite). Gated: fine rung dx = a/60 within **0.02** = round-up(measured envelope 0.0106 × 1.5) across 8 configurations (three apertures × centred/off-centre iris, plus two guide lengths — every configuration lands within 0.011), and the Richardson extrapolation 2·S(a/60) − S(a/30) on the oracle within **0.01** = round-up(envelope 0.0046 × 1.5) at every one of those pairs, which cross-confirms the oracle together with the first-order discretization attribution (gap ratios 0.407–0.440). Reported, **not** gated: the coarse rung (0.008–0.025), the raw-extraction record (worse than flux — gaps 0.009–0.025, pointwise difference up to 0.0068), and residual detrended ripple, measured against the oracle so its own curvature is not counted (fine ≤ 0.0076, coarse ≤ 0.0152). Three setup defects found during the campaign are each fenced by an assert or a derived setting: a parasitic wall-slot, half-ulp-fragile node-plane box corners, and a fin footprint that made the electrical aperture `d + 2·dx` — the last, together with a too-thin absorber, had inflated an earlier envelope 4–6×. The second of those is **inverted** by the lattice ownership contract: a PEC volume is sampled at cell centres, so a node-plane corner is now the well-defined position and a half-cell offset is the tie. The corners are back on the node planes and the footprint asserts read the realized edge set instead of a sigma mask. The earlier modal-extraction fence is **retracted**: its column-power 1.112–1.164 evidence does not survive the corrected setup (now 1.0012–1.0200, zero extractor warnings), and the withdrawn fence is replaced by a committed witness that records modal accuracy too — modal |S11| gaps come out at 0.0059–0.0269, comparable to flux and consistently a little worse at the same configurations, which is why flux still carries the gate. **Issue #812 re-gate, re-measured under #931:** the fine gate is **per configuration** — round-up of each configuration's own envelope × 1.5 at quantum 1000, i.e. 0.012/0.016/0.006/0.016/0.016/0.006/0.015/0.015, with the pooled **0.02** as the ceiling — because the pooled value was set by the worst configuration and then spent at all eight. All ten constants moved down or stayed when the thickness deficit closed (they were 0.019/0.034/0.015/0.022/0.035/0.015/0.034/0.034 over a pooled 0.04); that is the same rule re-derived on a corrected geometry, not a re-tuning. Aperture resolution is now stated rather than implied, and its numbers are committed in `validation/crossval/_18_wr90_iris_results/aperture_resolution.json` (re-derived from the committed traces by an independent oracle in the gate test) rather than restated here: a one-cell aperture error at each rung is now detected in **both** signs at every one of the eight configurations — at worst 1.623× the gate over-aperture and 2.608× under-aperture. Before the contract the under-aperture sign was caught at only two of eight and scored *better* than the undefected row at both d = 7.620 mm configurations. That asymmetry was never an aperture property: the committed fine trace's nearest oracle sat at d plus half a fine cell, which was the thickness deficit reading out on the aperture axis, the two being the only free dimensions of a symmetric iris. With realized thickness equal to drawn thickness the nearest oracle sits at the declared d at all eight, and nothing scores better defective than nominal. The Richardson witness is blind to the whole class in both signs *by construction* — a one-cell-per-rung error is proportional to dx, which is exactly what 2·S(a/60) − S(a/30) removes — so no tightening of its 0.01 gate could catch it. The calibration this stage supplies downstream is therefore aperture-resolved to one fine cell in both signs, and the three declared apertures are pinned as claims (an exact and even cell count at both rungs). **#931:** regenerated under the lattice ownership contract, and **every number in this row is that regeneration**. Before it, this case fed its mode-matching oracle the drawn iris thickness of 1.524 mm while the lattice realized one cell less — 0.762 mm at a/30, 1.143 mm at a/60 — because a body's far face was never a wall, and nothing in the case could see it: every assert counted masked planes, which agree with the drawing by construction. Under the contract the realized thickness is the drawn thickness, so the oracle's input is correct for the first time. Pass 1 (VESSL 369367259159) re-solved the migrated geometry with the pre-2.0 constants and exited non-zero by design, because the `--write-fixture` self-check demands every gate constant equal round-UP(its own envelope × 1.5); pass 2 (VESSL 369367259291, rc 0, 2026-09-07) is the identical command after each constant was re-derived by that rule, per configuration at quantum 1000, from pass 1's own envelopes. That arithmetic is the only thing that moved a number here — no window was widened by hand. The case also gains the contract's **one-cell volume witness**: an iris-thickness sweep t = 1..8 cells at a/30 against the lattice-blind oracle, gated on each rung identifying its own thickness (the oracle at t−1, t and t+1 cells, argmin on t). All seven rungs do, and at t = 1 the t−1 alternative *is* the pre-#931 realization — one wall, a zero-thickness screen — which comes out 4.32× worse (`one_cell_volume_witness.one_cell_two_wall_vs_one_wall_x` = 4.317), so the rule at one cell is settled by measurement rather than by convention. | `python validation/crossval/18_wr90_iris_modematch.py`<br />`pytest -q tests/crossval/test_wr90_iris_modematch_gates.py` |
| Diagnostic only | `19_wr90_iris_filter_aghanim` | A published 4th-order WR-90 inductive-iris bandpass filter (five irises, four coupled cavities), flux-normalized \|S11\| versus a mode-matching cascade oracle, 10.40–11.70 GHz (131 points at 10 MHz) | **TEn0 mode-matching cascade** with arbitrary aperture position, carrying the evanescent modes across every cavity. Self-witnessed (unitarity 2.2e-15, reciprocity and mirror symmetry exact, mode convergence 8.8e-4, an L → 0 collapse witness at 2.5e-05 measured), and its N=1 limit reproduces the stage-1 single-iris result to 1.05e-04 in an independent odd-mode formulation — the object a pre-merge review confirmed against a formulation-independent 2-D H-plane FDFD to 6e-4. Those witnesses have known limits: unitarity constrains only the propagating modes, mirror symmetry holds by construction here, and a formulation-independent check of the **five-iris** cascade now backs the oracle: a 2-D H-plane FDFD sharing only numpy/scipy with it agrees to 1.1 MHz in centre frequency and 1.0 MHz in bandwidth after first-order Richardson extrapolation over three refinement levels whose two extrapolation estimates agree to 0.4 MHz — a same-model consistency check, not an error bound: the three levels fit convergence order ≈1.25 rather than exactly 1, worth ~0.7–1.0 MHz of order-assumption uncertainty in the extrapolate, and f0 and bandwidth derive from the same two band edges, one confirmation rather than two — while rfx differs from both routes by the same ~12–13 MHz, placing that residual on the FDTD side. The published HFSS/CST curves are a digitized **anchor**, not a solver run — this case does not compare rfx against either commercial code. | The first resonant multi-obstacle case in the rectangular-waveguide lane: unlike a single iris, a per-face geometry error here is a passband shift rather than a magnitude tolerance. Gated: **centre frequency within 19 MHz** = round-up(envelope 12.1219 × 1.5); the structural reflection-zero **count**, witnessed invariant across the 8.0–8.5-cell iris-thickness ambiguity band by a committed oracle-side sweep; and passband contiguity as a regression lock (span_holes ≤ 1 — the f0 gate alone cannot see a split passband, since band edges are the outermost crossings), against the oracle on the as-realized geometry — measured **+12.12 MHz**, 3 zeros versus 3, one interior hole bin. The envelope is a population of **nine** configurations over four setup axes (guide height, run length, port standoff, absorber depth), each axis carrying an interior sample as well as an endpoint — an endpoint-only design cannot detect non-monotonic sensitivity, which is how PR #475 failed — spanning 12.1019–12.1219 MHz, a 0.02 MHz spread: the residual is a reproducible systematic difference, about 0.12 cell of cavity length at the measured −105 MHz/cell, not a setup artifact. Band edges (+17.05/+7.20 MHz) and bandwidth (−9.85 MHz) are **reported, not gated**, and they are one fact rather than two since the bandwidth residual is identically the difference of the edge residuals. The reason for not gating them changed with #931 and is now the third one to be given. The original reason — a ≈(t−0.68)·dx iris-thickness fit that matched neither integer rule and left a half-cell comparator-input uncertainty — is gone: it was the signature of a missing far face, not a convention. The second — that a gate needs a measured envelope and none existed — is discharged: pass 2 measured them, and both the envelopes (edges 17.0553 MHz, bandwidth 9.9024 MHz over the nine-configuration population) and the gates they would give (26.0 and 15.0 MHz) are committed with `applied: false`. What stands is that the population is **single-mesh**: every member is a/90, while lattice rounding is the dominant term for these two observables at 22–40 MHz per cell against f0's ~2.4 MHz, and the a/60 diagnostic rung reads +24.51/+15.24 MHz on them. A 1.5× lock over a population blind to the dominant term pins the mesh choice rather than bounding the solver, so re-gating needs its own pre-declaration and a cross-mesh sensitivity measurement. Before #931, handing the oracle the drawn counts instead of the realized ones biased f0 by +107.5 MHz; the current counts coincide. That historical example shows why the envelope×1.5 rule does **not** catch bias: it bounds scatter. Also reported: worst in-band return loss, ripple levels, every zero **depth**, contiguity detail beyond the span_holes lock (the built filter has one 10 MHz bin at 9.84 dB inside its 341 MHz span, longest contiguous run 270 MHz, where the oracle is contiguous), the coarse a/60 rung, and phase. Setup is gated separately from physics — a num_periods doubling with a column-power settling criterion (the committed gated leg also records energy settling of −59.23 dB for both drives; the setup gate still uses the run-length/column-power witness), feed clearance and absorber depth each hold the edges to one 10 MHz bin. The built structure is a **snapped** version of the published filter: centre frequency within the reference's own solver scatter of CST though not of HFSS, one of four structural zeros lost, and worst return loss degraded 13.82 → 10.65 dB by rasterization alone. Multi-iris filters, posts and septa remain experimental; this measures one published design on one mesh and supports no broader claim. **#931:** regenerated under the lattice ownership contract. The drawn cell counts are now the plain roundings — the case previously carried a +1 / −1 compensation that existed only to cancel a realization in which a body's far face was never a wall — and the built filter is unchanged by the deletion (same realized wall planes, same cavities, same apertures); the 8.0–8.5-cell "ambiguity band" is re-centred on the realized thickness. **Pass 2 has run and landed**, so the f0 gate, the envelope and the measured `d_f0` in this row are the post-contract record. The pre-declared falsifier was that `d_f0` stays at +12.08 MHz, a change of order 100 MHz meaning the redraw was not geometry-preserving; pass 1 (VESSL 369367259160) measured +12.12 and pass 2 (VESSL 369367259297, rc 0, 2026-09-07) reproduced it at the record's own 0.01 MHz precision, so the verdict does not depend on which pass is read. The 0.04 MHz that did move is the one deliberate non-preserved change: the metal span is now the true 276 cells rather than the compensated 277, so the trailing feed lost the extra cell it carried and the second reference plane sits one cell further into uniform guide. The f0 gate itself did not move — the re-derived `ceil(12.1219 × 1.5)` is 19.0, the value already in the source — and that rule is the only arithmetic that moved a number in this row. What the falsifier buys: this case's ~12 MHz unexplained rfx-versus-oracle residual is **not** a lattice-ownership artifact. It survived the contract intact, and the attribution question the record leaves open is now open against a geometry whose realized dimensions are its drawn ones. | `pytest -q tests/crossval/test_wr90_iris_filter_gates.py`<br />`python validation/crossval/19_wr90_iris_filter_aghanim.py` (≈19 min, excluded from the CPU lane) |
| Diagnostic only | `20_msl_phase_referee` | MSL de-embedded phase (`angle(S21)`) versus an external **OpenEMS** referee, on a matched-mesh (`dx = 50 µm`) RO4350B thru line, `W = 600 µm`, `h = 254 µm`, `L12 = 5 mm` | **OpenEMS** `MSL_NotchFilter.py` tutorial reproduce-gate (Stage A, against a Hammerstad–Jensen closed-form notch frequency) plus a matched-mesh thru-line comparison against rfx's own `compute_msl_s_matrix` (Stage B) | Answers whether rfx's un-gated MSL phase was a time-convention mismatch or a reference-plane mismatch: both tools' DFT kernels are identical at the source, so it was reference-plane, resolved by placing OpenEMS's measurement plane at rfx's own probe-0 coordinate. Gated: each solver's own `angle(S21)` against its own measured `beta` (self-consistency, a 3° budget from a ±4-cell plane-position allowance, resolving-power tested against a planted referral defect), plus passivity and settling guards, over 3.0–4.5 GHz. Also gated since 2026-09-01 (issue #812): the raw cross-solver `angle(S21)` difference itself, at 3°, measured `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.run2_realized_board.cross_solver_max_abs_raw_phase_diff_deg = 0.3418` degrees inside the gated band on the historical pre-#931 run-2 pair. The committed replay with the current rfx leg and historical run-2 OpenEMS leg reads `cv20.run2_openems_with_current_rfx_fixture.cross_solver_max_abs_raw_phase_diff_deg = 0.5308` degrees; the corresponding analytic-beta errors are 1.4122% rfx and 0.3068% OpenEMS. This replay is not a fresh matched-board OpenEMS solve after #931; that rerun remains outstanding. It was reported-only before that date, which mattered because the self-consistency leg alone cannot fail on a coherent phase-velocity error — a factor-2 error reads `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.blindness.audit_construction_e1_max_phase_dev_deg = 0.0647` degrees against its 3° gate. The 500 MHz bin is excluded from any claim (the 5 mm line is ~0.03 guide wavelengths there and three-point port extraction is ill-conditioned). Reported, not gated: the dispersion-corrected residual, which is provably blind to that same error class. OpenEMS's `CalcPort` reference-plane-referral rotation is unexercised on this fixture (the effective shift measures `0.0` by construction) and remains untested off-grid. | `python validation/crossval/20_msl_phase_referee.py`<br />`pytest -q tests/crossval/test_msl_phase_referee_header.py` |
| Diagnostic only | `21_coax_two_port_referee` | Coax two-port through-line `\|S21\|`, phase, and group delay versus an external **OpenEMS** referee, on a matched-mesh (`dx = 0.37474 mm`) SMA-class PTFE coax (`a = 0.635 mm`, `b = 2.055 mm`, `eps_r = 2.1`, `L12 = 58.4595 mm`) | **OpenEMS** `Coax.m` tutorial reproduce-gate (Stage A, against Coax.m's own closed-form line-impedance check) plus a matched-mesh `CoaxialPort` thru-line comparison against rfx's own `compute_coaxial_two_port` fixture (Stage B, forward + reverse drive, full S-matrix) | Comparator-leg referee: it brackets rfx's own numbers, it does not judge them. `compute_coaxial_two_port` is **VALIDATED WITH SCOPE** — through-line class, this coax geometry family — and this run is one leg of that evidence chain (#489, 2026-08-06). `docs/guides/sparameter_support_matrix.md` carries the full scope statement; coax-to-planar transitions stay EXPERIMENTAL, diagnostic-only. Gated: Stage A reproduces Coax.m's own documented `ZL` check (measured 50.432 Ω vs analytic 49.940 Ω, max dev 0.511 Ω); Stage B gates each solver's own self-consistency — passivity (`\|S11\|² + \|S21\|² ≤ 1.05`, measured `≤ 1.0001`), a one-sided matched-through magnitude band (`[0.5, 1.1]`; the gated central-band leg measured `0.9999966–1.0000282` combined across `S21`/`S12`), and a phase/group-delay witness against the port's **own measured** `beta`, not an idealized analytic one: the analytic-beta path fails by 111.38° (>30° tolerance) even though its own magnitude and group-delay legs pass (282.6 ps expected, 35.4 ps deviation, inside a 200 ps tolerance) — switching to the port's own measured `beta` collapses the same run-3 data to `< 0.44°` phase deviation on both `S21` and `S12`. This is staircase dispersion (the referral hypothesis is ruled out by the implied-length mismatch matching no candidate discrete referral-cell count) from this coax's own coarse a-to-b PTFE annulus (measured/analytic `beta` ratio `~1.12×`, 1.1205–1.1212 in the gated central band; ~3.8 cells across) — **measured by a mesh-refinement convergence witness** (issue #489 leg 1, VESSL `369367251845`, 2026-08-05, one-sided acceptance band pre-declared before the run): a 1.5× mesh refinement (annulus 3.79 → 5.68 cells) moves the measured/analytic `beta` ratio from `1.1208` to `1.0662` (implied convergence order `p ≈ 1.5` (two-point, from this single 1.5× step — not a multi-level fit), between the naive first-order and second-order bounds — consistent with openEMS's own material averaging at a dielectric interface, not a fault) — the pre-declared one-sided acceptance band **CONFIRMED**. The four agreeing `beta` readings (port1/port2, both drives) at the original mesh density remain one witness, not four (same run, same solver, same `CalcPort` extractor); the mesh-refinement run above is the independent second axis. Reported, not gated: run-3's own `\|S21\|` ≈ 1.000 (`0.99997–1.00003`) across the full 4–12 GHz sweep for this independent, differently-discretized geometry — consistent with, not a refutation of, rfx's own raw `0.960 → 0.737` decline (a different geometry/discretization); reciprocity (`\|S21\|` vs `\|S12\|`, max magnitude deviation `1.2 × 10⁻⁵`, max phase deviation `0.0012°` — essentially exact for this passive fixture). Does not characterize a different geometry, mesh density, coax-to-planar transition, AD, or the `reverse_drive` call-site wiring (the active #489 dev track). | `python validation/crossval/21_coax_two_port_referee.py`<br />`pytest -q tests/crossval/test_coax_two_port_referee_header.py` |

## Case-specific cautions

### `05_patch_antenna`

The OpenEMS comparison requires CSXCAD and openEMS. If either is unavailable,
the script exits `2`: the rfx Harminv and lumped-port `\|S11\|` self-checks run,
but the OpenEMS comparison does not.

The single-cell lumped port has parasitic reactance. Use the Harminv frequency
as the resonance result. The shallow local `\|S11\|` dip is only a passivity
and local-dip check; it is not an absolute feed calibration.

The script's own OpenEMS comparison is coarse (no thirds-rule edge mesh) and is
reported, not gated. A separate committed reference from the canonical
thirds-rule recipe (`scripts/diagnostics/patch_tutorial_openems.py`; f_res
2.4221 GHz, Q 20.1, broadside D 6.79 dBi, from our source-built openEMS run —
full provenance in the fixture `meta` block) is locked by
`tests/crossval/test_patch_canonical_farfield_e4.py`. Those reference numbers are
the 32 mm feed-axis mode, and the rfx side gated against them is that same design
mode — the upper ring-down mode, 2.5070 GHz on the #931 sheet-declared board —
not the 40 mm cross mode.

{/* constants: tests/crossval/test_patch_canonical_farfield_e4.py, by NAME —
     D_ABS_TOL_DB, PEAK_ANGLE_TOL_DEG, F_RES_REL_LO, F_RES_REL_HI,
     SETTLING_BAR_DB; line numbers have gone stale before. Measured values
     re-read 2026-09-07 from the #931 re-derivation record
     tests/fixtures/patch_canonical_farfield_e4/canonical_farfield_e4_measured_369367259302.json
     and from that test file's own constant comments. */}

| Gate | Constant | Measured |
|---|---|---|
| broadside directivity | `D_ABS_TOL_DB = 1.0` | 0.0659 dB at the design-mode bin (6.7241 vs 6.79 dBi) |
| E-/H-plane beam peaks | `PEAK_ANGLE_TOL_DEG = 15.0` from broadside | −1° / −4° (openEMS 0° / 0°) |
| resonance at dx = 2 mm | `F_RES_REL_LO = -0.02`, `F_RES_REL_HI = +0.09` — the band straddles zero and is **not** sign locked | +3.51% |
| ring-down settling | `SETTLING_BAR_DB = -40.0`, asserted before any gated number | −42.9 dB at the `num_periods = 110` the test runs |

Every measured value in that table comes from the sheet-declared board of VESSL
369367259302. The pre-#931 board read 0.60 dB, 0° / −3°, and `[+0.06, +0.16]`
around +11.3%. Its `num_periods = 90` demo settled to −36.5 dB, just under the
bar, which is why the test runs 110 periods; that 90-period demo has not been
re-run on the sheet board, so −36.5 dB stays a pre-#931 figure.

The frequency band is a discretization-bias regression lock, not an accuracy
claim. It is pinned against the design mode — the 32 mm feed-axis mode, not the
40 mm cross mode, which reads 2.0346 GHz on this board (#693, 2026-08-27; #812;
#931).

The band no longer carries a sign, and that is a change of kind rather than a
retune. `[+0.06, +0.16]` excluded zero, so it asserted "rfx reads high here" as
part of the characterization; `[-0.02, +0.09]` contains zero and asserts only
that the coarse-dx offset stays inside the measured +3.51% ± 5 percentage
points, rounded outward to whole percent — the same rule that shaped the old
band around +11.3%. What moved is the conductor realization, not the mesh.
Before 2.0 the same geometry could be run under two different realizations of a
one-cell PEC box, which disagreed by about one cell of substrate height, and
this fixture's ground was the realization whose own cell sat inside the modelled
cavity as vacuum. That choice no longer exists — a conductor is a sheet or a
volume by declaration, this fixture's ground and patch are foil and hence
sheets, and the offset fell from +11.3% to +3.51%.
{/* mechanism: the comment block above F_RES_REL_LO / F_RES_REL_HI in tests/crossval/test_patch_canonical_farfield_e4.py (cite the constant names; line numbers have gone stale before).
     validation/crossval/manifest.json records the older cv05 agreement as
     "a documented z-under-res/staircase error cancellation".
     The pre-2.0 one-plane / two_plane comparison is dated history, kept in the
     CHANGELOG's #740 and #931 entries; it is not re-measurable under the
     ownership contract. */}

### `06b_msl_notch_filter_uniform`

The script applies loose analytic tolerances to the uniform-mesh
`add_msl_port` result. It runs at dx = 63.5 µm = h_sub/4, chosen so the
254 µm substrate rasterizes exactly; the analytic quarter-wave reference is
evaluated on the realized trace width read from the run's own
`fidelity_report()`, not on the declared 600 µm. (The realized width is a
property of the mesh and the sampling rule, so it is read from the run rather
than quoted here; it changed at 2.0 with the conductor realization.)

The committed dx=50 µm rfx/OpenEMS comparison is checked separately by
`tests/crossval/test_msl_notch_e4_comparison_gates.py`. It covers that geometry and
resolution only, not arbitrary MSL structures or meshes — and it is **not the
same board**: its rfx leg is generated at dx = 50 µm, where the same 254 µm
substrate rasterizes to 300 µm. Until that generator is regenerated, read the
two as characterizing different boards.

### `10_pmc_cpml_half_symmetric`

This is a regression check with no external solver. It verifies only that
far-face absorber thickness does not materially change the interior peak for
the scripted uniform and nonuniform cases.

### `11_waveguide_port_wr90`

This script prints a diagnostic report. The
empty-guide and PEC-short magnitude criteria are direct checks. The dielectric
slab phase and complex-S tolerances account for different analytic and
solver-monitor reference planes and therefore must not be read as general
phase-accuracy limits. The regression checks are in
`tests/oracle/test_waveguide_port_validation_battery.py`.

Missing optional external result files do not skip the analytic checks. Read
the per-frequency table before interpreting a passing exit code.

## Cases not included

Research-only subgrid scripts are under `validation/research/subgrid/`. They are
not in the cross-validation manifest and are not documented as supported
reference cases.

## Reproduce the CPU subset

Run the CPU-feasible cases with:

```bash
PYTHONPATH=. python scripts/run_crossval_cpu.py
```

The runner starts each script in a fresh subprocess, applies a per-script
timeout, and prints one of these results:

| Result | Meaning |
|--------|---------|
| `PASS` | The script returned `0` and no failure sentinel was found. |
| `SELF-CHECK-ONLY` | The script returned `2`; required external evidence is unavailable, so the result is inconclusive. |
| `FAIL` | The script returned `1`, another unexpected exit code, or a contradictory failure sentinel. |
| `ENV-SKIP` | An optional reference solver such as Meep is installed but cannot be imported. This is an environment or packaging problem, not an rfx result. |
| `TIMEOUT` | The script exceeded its configured time limit. |
| Validated comparison | `26_oblique_slab_fresnel` | Oblique-incidence slab `R(f)`, `T(f)` (ε = 4, 10 mm, cv04's slab) on the Bloch-periodic TFSF path, each bin at its realized angle of a fixed transverse wavenumber: TE 0–70°, TM (via the exact ε↔μ duality, Brewster included) 37–70°, and the CPML at 80–85° on a compact box. Scope: one slab, one ε, 2-D, fixed-k_y Bloch TFSF only; p-polarization is not injectable by either rfx oblique TFSF path. | Analytic Fresnel / transfer matrix at the realized angle (TE, TM) and Meep with a `k_point` Bloch boundary (Ez / Hz) are gated; the exact 2-D Yee-lattice solution (rfx's own CPML recursion included) gates the grazing arms and is reported on every arm. | Per bin and band-mean `\|R − R_Fresnel(θ(f))\|`, `\|T − T_Fresnel(θ(f))\|`, closure and passivity inside cv04's committed windows plus the derived numerical-dispersion and injection terms; the Brewster bin; the vacuum-arm leakage bar; the grazing PEC arm's excess `R − 1` against the exact lattice within 0.5× the a-priori 3-D absorber term + 2e-3. Prose only until the VESSL round lands (`docs/design_notes/20260902_cv26_oblique_fresnel_predeclaration.md`). | `python validation/crossval/26_oblique_slab_fresnel.py` |

The runner exits `0` only when no script ends in `FAIL` or `TIMEOUT`.

The CPU runner does not attempt these cases:

- `06b_msl_notch_filter_uniform`: GPU only. Measured, not projected: a CPU run
  of the shipped dx = 63.5 µm mesh was abandoned unfinished at 2 h 52 m, while
  a single RTX 4090 solves it in 329 s. Do not budget a CPU slot for it.

Cases `01` through `04` require a Meep build compatible with the host NumPy ABI.
If Meep cannot be imported, the runner reports `ENV-SKIP` instead of `FAIL`;
cases `01` and `04` still complete their rfx-side self-checks.
