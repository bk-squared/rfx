---
title: "Benchmarks"
description: "Reference cases, pass criteria, required dependencies, and reproduce commands for rfx cross-validation."
sidebar:
  order: 8
---

import { Aside } from '@astrojs/starlight/components';

For validation scope and recommended engineering checks, read
[Cross-Validation and Accuracy](/rfx/guide/validation/).

`validation/crossval/manifest.json` defines the case list, dependencies, exit
codes, checks, and saved artifacts. Automated tests compare the table below
with that manifest, so a case cannot be added or reclassified without a
documentation update.

<Aside type="note" title="Pass criteria are case-specific">
  The metrics and tolerances below come from each script's docstring and check
  block. A pass applies to the stated geometry, mesh, frequency range, and
  reference only. It does not by itself validate other geometries, arbitrary
  discretizations, or an entire port or component family.
</Aside>

<Aside type="caution" title="Conductor cases are recomputed at 2.0">
  Release 2.0 changed how a conductor lands on the lattice: a PEC volume
  realizes walls at both of its drawn faces, and foil is declared as a sheet on
  one node plane
  ([How conductors land on the lattice](/rfx/guide/materials-geometry/#how-conductors-land-on-the-lattice)).
  Every case below with a declared conductor body — `05`, `07`, `11`,
  `15`, `18`, `19`, `20` — is re-solved from the drawn geometry, with the
  compensations that used to sit in its oracle deleted rather than re-tuned;
  a case that has not been re-solved does not ship in 2.0.
  `validation/crossval/manifest.json` and each case's committed artifact are
  the live source for a gate value; a number quoted in this table and not in
  the manifest is documentation, not the gate.

  Three groups of cases are controls and must come back unchanged: the
  boundary-PEC cases (`09`, `10`, `14`, `24`), the dielectric-only cases
  (`01`–`04`, `17`, `22`, `23`), and the σ-fill cases (`16`, `21`), whose metal
  is a conductivity written into cells rather than a declared conductor and is
  fenced out of the contract.
</Aside>

## Result codes

Each script returns one of these exit codes:

| Exit | Meaning |
|------|---------|
| `0` | Every self-check and numeric criterion passed, including each reference marked `required_for_script_pass` in the manifest. |
| `1` | A required run, self-check, or numeric criterion failed. |
| `2` | The rfx self-check completed, but a required external reference or optional dependency such as OpenEMS was unavailable. This result is inconclusive, not a pass. |

Some manifest entries also define a failure-line sentinel. The CPU runner
checks that sentinel after exit `0` and treats a contradictory failure message
as a failure.

## Reference cases

**Validated comparison** means that the listed command or commands check the
references explicitly identified as gated in that row.
**Diagnostic only** means that the script reports a comparison for the stated
configuration or checks a specific regression condition; it must not be used
as evidence for broader support.

| Use | Case | Purpose | Reference | Pass criteria | Reproduce |
|-----|------|---------|-----------|---------------|-----------|
| Diagnostic only | `07_sheen_lpf` | Sheen 1990 stepped-impedance microstrip low-pass filter stopband | **OpenEMS** matched-geometry run, whose own passivity and `Re(Z₀)` sanity are gated first, plus a committed **Palace** FEM conformal-mesh referee (`tests/fixtures/sheen_lpf_e4/`) | Characterization locks only, no accuracy gate. The referee shows the stopband is a double transmission zero (~7.0 and ~8.0 GHz) that OpenEMS matches to ~0.7%. The rfx leg was regenerated 2026-07-26 with the measured recipe (num_periods = 60, settling witness −72 dB vs the −24.7 dB truncation failure of the original num_periods = 20 leg; n_probe_offset = 30, which removes a measured probe near-field contamination — moving the rfx argmin from 7.218 to 7.874 GHz, the ~8 GHz doublet member, 1.4% from OpenEMS — while keeping the λ_g/4 reflector clearance that offset = 40 violated). The current #931 sheet leg and derived referee were regenerated by VESSL 369367259192 (2026-09-07, commit 069bdebc): raw-bin rfx argmin 8.202 GHz vs OpenEMS 7.983 GHz (2.74% split); parabolic FDTD argmins 8.244069 / 7.994749 GHz; structure distance 2.87% vs OpenEMS 0.66%, distinct from the separately windowed rfx argmin distance to Palace of 2.3756%. Quoted rfx `\|S\|` is passivity-enforced because this case asks for it (`enforce_passivity=True`; the API default returns the raw extraction) (strict `≤ 1`, max column power 0.9995); the raw excess is recorded per bin in `passivity_correction` (3/120 bins > 0.05 on the current leg, worst 0.6572 at 17.870 GHz; all three are above 17 GHz, none inside the 5–15 GHz null band. The case characterizes this as a high-frequency residual on a coarser realized strip at the same dx = 200 µm, not evidence of a physics regression in the stated null band; no magnitude-accuracy claim is made — the pre-#516 leg's 84/120 bins, worst 0.365 at 18.0 GHz, was substantially extractor defects, not a coarse-mesh envelope). The fitted passband median `Re(Z₀)` (51.9 Ω vs OpenEMS's ~51 Ω) is printed, not gated — the old ~67 Ω fit-conditioning artifact is gone; the in-band 5–15 GHz median is 54.7 Ω. Gated: OpenEMS reference passive (column power `≤ 1.10`), median `Re(Z₀) ∈ (40, 65) Ω`, passband mean `\|S21\| ≥ 0.85`, stopband zero `≤ −20 dB`; committed bin counts; the committed argmin nulls to `1%`, the rfx passband mean to `1%`, and the referee verdict; and the evidence chain (strict bound, settling witness < −40 dB, correction footprint) failing closed when a leg lacks the witness/enforcement fields. Re-gated 2026-09-01 (issue #812 mechanism P3, `docs/design_notes/estimator_resolution_regate.md`): the argmin null was bin-quantised at 163.866 MHz = 2.081% per bin against a declared 1% window, so erasing one of the two transmission zeros passed all 17 gates. The argmin lock is kept and four families were added — both doublet members per solver located by sub-bin log-parabolic vertex and locked to `±0.50%` against the committed referee fixture's own values; the transmission-zero COUNT (`== 2`, depth `≤ −20 dB`); the `−3 dB` corner frequency (`±0.25%`), which this case never gated; and an in-run half-grid resolution witness that a bin-quantised estimator scores exactly `1.0000` bin on and cannot pass. The #931 regeneration records 28/28 passing gates on the committed legs; the estimator-falsifier fixture remains a pre-#931 calibration, so it does not certify the regenerated leg's defect response. | `python validation/crossval/07_sheen_lpf.py`<br />`pytest -q tests/crossval/test_sheen_lpf_palace_referee_gates.py` |
| Diagnostic only | `15_patch_antenna_rt5880` | Probe-fed 40×50 mm patch on RT/Duroid 5880 (`eps_r` 2.2, h = 3.175 mm), uniform-mesh integration study | **OpenEMS** same-geometry run is gated; its canonical "Simple Patch Antenna" tutorial reproduce-check and a **Balanis** transmission-line estimate are reported, not gated | Same-geometry integration envelope, not a patch-accuracy claim: rfx ring-down `f0` versus the OpenEMS `S11` dip within `8%` (issue #812: blind to the #740 one-plane-ground realization, `validation/crossval/_15_patch_results/rfx_one_plane_ground_b29f9de7.json::f_harminv_hz = 2471858108` Hz vs `validation/crossval/_15_patch_results/openems.json::f_dip_hz = 2330000000` Hz, +6.09 % — only the build assertion catches it; a ratio-band re-gate was tried and withdrawn as an honest STOP; **that realization is pre-2.0** — from 2.0 a conductor is a volume, a sheet or a wire by declaration and this ground is a sheet on the substrate floor, so the cited figure is the dated record of what the old realization cost and is not reproducible on the current tree. The 8 % gate's blindness is a property of the gate and is unchanged by that), the −40 dB open-domain settling witness, passivity on both legs (`max\|S11\| ≤ 1.05`), and broadside directivity within `3 dB`. The substrate is asserted to rasterize to exactly four z-cells on the uniform mesh before any `f0` is quoted. Patch evidence remains delegated to the committed tests the former cv05 patch case named (regression locks and characterizations, not accuracy claims). | `python validation/crossval/15_patch_antenna_rt5880.py` |
| Diagnostic only | `20_msl_phase_referee` | MSL de-embedded phase (`angle(S21)`) versus an external **OpenEMS** referee, on a matched-mesh (`dx = 50 µm`) RO4350B thru line, `W = 600 µm`, `h = 254 µm`, `L12 = 5 mm` | **OpenEMS** `MSL_NotchFilter.py` tutorial reproduce-gate (Stage A, against a Hammerstad–Jensen closed-form notch frequency) plus a matched-mesh thru-line comparison against rfx's own `compute_msl_s_matrix` (Stage B) | Answers whether rfx's un-gated MSL phase was a time-convention mismatch or a reference-plane mismatch: both tools' DFT kernels are identical at the source, so it was reference-plane, resolved by placing OpenEMS's measurement plane at rfx's own probe-0 coordinate. Gated: each solver's own `angle(S21)` against its own measured `beta` (self-consistency, a 3° budget from a ±4-cell plane-position allowance, resolving-power tested against a planted referral defect), plus passivity and settling guards, over 3.0–4.5 GHz. Also gated since 2026-09-01 (issue #812): the raw cross-solver `angle(S21)` difference itself, at 3°, measured `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.run2_realized_board.cross_solver_max_abs_raw_phase_diff_deg = 0.3418` degrees inside the gated band on the historical pre-#931 run-2 pair. The committed replay with the current rfx leg and historical run-2 OpenEMS leg reads `cv20.run2_openems_with_current_rfx_fixture.cross_solver_max_abs_raw_phase_diff_deg = 0.5308` degrees; the corresponding analytic-beta errors are 1.4122% rfx and 0.3068% OpenEMS. This replay is not a fresh matched-board OpenEMS solve after #931; that rerun remains outstanding. It was reported-only before that date, which mattered because the self-consistency leg alone cannot fail on a coherent phase-velocity error — a factor-2 error reads `validation/crossval/_issue812_phase_identity/regate_evidence.json::cv20.blindness.audit_construction_e1_max_phase_dev_deg = 0.0647` degrees against its 3° gate. The 500 MHz bin is excluded from any claim (the 5 mm line is ~0.03 guide wavelengths there and three-point port extraction is ill-conditioned). Reported, not gated: the dispersion-corrected residual, which is provably blind to that same error class. OpenEMS's `CalcPort` reference-plane-referral rotation is unexercised on this fixture (the effective shift measures `0.0` by construction) and remains untested off-grid. | `python validation/crossval/20_msl_phase_referee.py`<br />`pytest -q tests/crossval/test_msl_phase_referee_header.py` |

## Cases not included

Research-only subgrid scripts are under `validation/research/subgrid/`. They are
not in the cross-validation manifest and are not documented as supported
reference cases.

## Reproduce the CPU subset

Run the CPU-feasible cases with:

```bash
PYTHONPATH=. python scripts/run_crossval_cpu.py
```

The runner starts each script in a fresh subprocess, applies a per-script
timeout, and prints one of these results:

| Result | Meaning |
|--------|---------|
| `PASS` | The script returned `0` and no failure sentinel was found. |
| `SELF-CHECK-ONLY` | The script returned `2`; required external evidence is unavailable, so the result is inconclusive. |
| `FAIL` | The script returned `1`, another unexpected exit code, or a contradictory failure sentinel. |
| `ENV-SKIP` | An optional reference solver such as Meep is installed but cannot be imported. This is an environment or packaging problem, not an rfx result. |
| `TIMEOUT` | The script exceeded its configured time limit. |

The runner exits `0` only when no script ends in `FAIL` or `TIMEOUT`.

Cases `03` and `04` require a Meep build compatible with the host NumPy ABI.
If Meep cannot be imported, the runner reports `ENV-SKIP` instead of `FAIL`;
case `04` still completes its rfx-side self-checks.
