"""Tier-3 crossval gates: tests whose subject is a validation/crossval case --
its committed reference fixture, its gate arithmetic, its referee script's
header/record, or its broad-E4/E5 envelope -- and that would move if the
case moved. Regression locks live in ``tests/locks/``; documentation /
evidence-integrity contracts in ``tests/contracts/``; unit tests of rfx
functions stay in ``tests/``.
"""
